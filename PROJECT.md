# Onduler — State of the Project

*Last updated: 2026-06-19*

## The big picture

**Onduler** (*on-doo-LAY* — French for "to wave") is a gamified personal fulfillment app built around points-based habit tracking across life domains. The goal is to bring balance to life by helping you track where you spend your energy. Domain owned: **onduler.app**.

## The core product insight: waves and tides

Most habit apps treat every day like it should look the same. Onduler doesn't. The app recognizes two modes a person can be in, and meets them where they are. Every interaction with Onduler should feel like a celebration of the user showing up — for themselves, for their domains, for their life. This is the bar every design decision gets measured against. If a feature makes the user feel watched, judged, or behind, it doesn't ship, even if it would drive engagement metrics. The competition optimizes for retention through guilt; Onduler optimizes for retention through joy.

- **Waves** = Human energy and capacity cycle. Sometimes a wave is a creative obsession. Sometimes it's a depressive episode that keeps you in bed for a week. Sometimes it's grief, illness, a new baby, a hard season at work. Onduler treats them the same: a wave is whatever's pulling you under right now, and the app's response is to not chase you. We'll be here. Take care of what you need to take care of. When you surface, the tide is gentle — "hey, want to try knitting today?" — and the app meets you wherever you've landed. The product never uses the language of failure, deficit, or falling behind. Showing up at all is honored.

- **Tides** = the steady, default rhythm. When you're not on a wave, the app gently helps you direct your energy across domains so nothing atrophies. This is where the gamification lives — points, hours, swells.

## Who this is for, and the feeling

*Brand soul, crystallized in a 2026-06-15 positioning session. This is the why behind the mechanics below. Where it bears on user-facing copy, it interacts with the Vocabulary and Working agreements rules.*

**Who.** Searchers. People who want a fuller, more alive life but are allergic to the optimization machinery that usually comes with it. Creatives, people carrying mental illness, soft souls who feel itchy in their own skin and try on other selves for relief. People who don't yet know what brings them joy, or who they are under what they think they should be. This is a temperament, not a demographic, so we reach them by resonance and self-selection, never by targeting. The optimizer who wants streaks and PRs is not the user; copy should let that person feel nothing and leave.

**The feeling: relief, then pride.** First contact should produce calm, specifically calm as relief, the exhale of a space where you cannot fall behind and nothing judges you. Not spa-stillness; calm in motion, like a sea that is calm but always moving. Over time the second feeling arrives on its own: pride, the quiet look back at what you slowly built. The whole job is to carry someone from relief to pride without ever using pressure. The competition manufactures pride through pressure and delivers neither.

**A refuge, and a mirror with no opinion.** Onduler is a private space that is only yours. It reflects what you actually did and lets you decide whether you like what you see. It holds no opinion about whether your life is good enough. The judge is never the app, it is your own future self; your change over time becomes the opinion. This is motivational interviewing: ask the question, hold the data, let the gap between who you expected to be and who you actually were do the work. The Anchors surface is where this lives.

**What it's really about: core, not joy.** Under the word "joy" the real thing is identity: noticing what is load-bearing in who you are (the roots you want to sink, which is what an Anchor already means in our water vocabulary), and noticing when you are drained by being someone you're not. The honest test is not "did this spark joy," it is "am I sustaining a self that isn't mine." Behavior shows what you did, not why, so the reflection layer is load-bearing: the data shows the drift, the gentle question makes someone feel the drain they'd been ignoring.

**The moat: no other consciousness in the room.** No AI watching, no human watching, no algorithm scoring. As life gets more observed and more AI-evaluated, an unwatched space becomes rare, and that value is the absence of AI, which AI cannot replicate. This is a hard constraint, not a tagline. Never put an AI coach inside the refuge, even when competitors ship one. The someday insight engine must stay a mirror, never a coach: it can show you where your time went, it can never tell you what that means. Everything in the refuge reflects, nothing judges. This is why AI stays bring-your-own (see the AI-import decision): the user carries data out to their own AI if they want, the room stays empty.

**Two products, one leads.** The headline is the over-time engine: a self-reflection app that shows you what your daily actions are actually building. The second, equally welcome, is the sparse memento: someone who uses it for a month at a fork and treasures it like a half-filled journal. Never imply the sparse user failed. But the brand leads with the engine, both because that is where the most value sits and because the longitudinal magic (what stayed, what drifted, what was absent when you felt off) needs years of data, which only the gentle no-decay design buys time to collect. All are welcome; some who get the most will make one realization and never return, and that is a win.

**Plain-language floor.** A stranger gets five seconds against a wall of invented words (motions, swells, tides, wakes, anchors). The one thing they must grasp in plain language: a self-reflection app that shows you what your daily actions are actually building. Everything else may stay mysterious. The legibility we need is emotional ("this won't make me feel behind"), not functional. Never call it a habit tracker in user-facing copy; "habit tracker" is the scratchy sweater this is the relief from. Register is cozy game, not RPG grind. The skill-tree / character-sheet framing was a useful Reddit traffic hook but is being retired: it recruits optimizers and slightly contradicts the noticing-not-declaring soul.

**The checklist is not the enemy.** The satisfying gesture of checking something off was never the problem; the guilt stapled to the unchecked box was. Keep the gesture, strip the shame: a list with no penalty for what stays blank is just satisfying. Self-selection is fine. If you don't like checklists this probably isn't for you, and the weekly calendar is the alternate door.

**Duty of care (the mirror as cudgel).** The audience includes people in dark places, and depression reads neutral data as indictment. The same honest reflection that gives one person relief can hand another ammunition. Adjustable targets are the primary safeguard: meet yourself where you are, a depressed week can be "brush teeth, get out of bed," target of 1, and that counts. The gentler the activation the better; ideally the bar meets the user without their having to ask on the worst day (cf. the welcome-back ramp). Guard against a bad-day user opening to yesterday's ambitious targets and a wall of unchecked motions.

**Privacy is the load-bearing wall (leaning, not yet locked).** "No observer in the room" only works as a moat if it is literally true. Litmus: Josh should be able to tell his own family to use it and mean it that he will never see a word they write. That makes privacy an architecture requirement (build it so you cannot read user content), not a promise to keep. Leaning direction, still under review: unwatched by default; per-user insight computed and shown only to that user (the mirror, on-brand); behavioral/usage analytics for the business (counts, retention, funnel, no content); content analysis rare and opt-in. De-identification helps aggregate research but is weak for life-logs (patterns re-identify) and still means someone read it, so consent-first and minimal beats de-id-and-hope. Before building any analysis layer or writing privacy-policy copy, get a real privacy review; the audience may log mental-health-adjacent content, which can be legally sensitive. Not yet a locked decision.

**Open copy thread.** The "you are never locked in, change is good, start a new chapter" reassurance currently appears only at the cycle-close ceremony, a year in. It should also land on the first onboarding screen, which today opens cold on a task ("Choose your swells, add at least 3") with no welcome. Surface the never-locked-in relief at first contact.

## Vocabulary (user-facing — never deviate)

| Term | Meaning |
|---|---|
| **Motion** | A trackable daily activity (the code table is also `motions`) |
| **Swell** | A noun-shaped area of life the user wants to invest in (e.g. Movement, Home, Nutrition, Family, Creativity, Reflection). Has a weekly points target or weekly hours target. Celebration fires each time cumulative weekly progress crosses the target; the cycle resets at Sunday 12:00 AM Pacific (Sun→Sat week). Motions can belong to many swells. **Mental model: swells are nouns, motions are verbs.** See ADR 0002. |
| **Group** | An organizational bucket for motions and swells. No target, no scoring. Each motion and each swell belongs to one group (or none). |
| **Tide** | Default daily mode |
| **Wave** | Period of focus, recovery, or disruption — app does not punish this |
| **Waypoint** | A user-authored marker within a swell — a point the user is navigating toward inside the swell's ongoing rhythm, not an endpoint of it. Two kinds: recurring (e.g. publish weekly) and one-shot (e.g. start a band). Completing a waypoint celebrates and may add bonus points to the swell. Internal table name remains `milestones`. See ADRs 0004 and 0006. |
| **Anchor** | Both the surface where the user notices and adjusts themselves (formerly Reflections — see ADR 0008), and an entry written there. The cycle-close ceremony writes an anchor; the `+` button drops a free-form anchor any time. The framing: swells anchor you to who you are; the surface is where you check whether your motions still align with them, and drop a marker each time you do. Internal table name remains `reflections`. |
| **Wake** | The shoulder-polygon outline of motions actually logged across a cycle. Monochrome, breathing, no swell colors. Coalesces from a pulsing circle as the user logs; hidden during wave mode but reappears the moment they log within the wave. Lives on the locked Anchors page across all four cadences. Marketing surfaces use a seeded random version. See ADR 0010. |

**Never use:** tasks, activities, goals, domains

## Tech stack (locked)

| Layer | Choice |
|---|---|
| Framework | Next.js (App Router) |
| Language | TypeScript |
| Database / Auth | Supabase (Postgres + Auth) |
| Hosting | Vercel |
| Styling | Tailwind CSS |
| Payments (future) | Stripe |
| Source control | GitHub (`underparachutes`) |

## Data model

| Table | Key columns | Notes |
|---|---|---|
| `motions` | id, user_id, name, default_points, default_hours (default 1.0), group_id (nullable), parent_id (submotions), hidden, sort_order | parent_id enables submotions; hidden removes from daily checklist; group_id is the (single, optional) organizing folder |
| `swells` | id, user_id, name, color, target_points (nullable), target_hours (nullable), group_id (nullable), color_picked_in, sort_order | Either/both targets optional; group_id is the (single, optional) organizing folder |
| `groups` | id, user_id, name, color, color_picked_in, sort_order | Organizational only, no targets; shared across motions and swells |
| `motion_swells` | motion_id, swell_id, contribution_weight | Junction: many-to-many, weighted |
| `logs` | id, user_id, motion_id, points, hours, intensity, logged_at | hours defaults to motion's default_hours at log time; intensity in (light, medium, deep) DEFAULT 'medium' — multipliers (0.5×/1×/1.5×) applied to points and hours at log time |
| `milestones` | id, user_id, swell_id, name, kind, cadence, target_count, completed_at, bonus_points, sort_order | User-facing name: **Waypoint** (table stays `milestones`, see ADR 0006). kind in (recurring, one_shot); recurring uses cadence + target_count; one_shot uses completed_at. See ADR 0004 |
| `wave_checkins` | id, user_id, energy, alignment, duration_seconds, checked_in_at | |
| `user_settings` | user_id, groups_enabled, daily_goal, daily_goal_hours, tracking_mode, theme, celebration_enabled, haptic_enabled, onboarding_complete | |
| `chapters` | id, user_id, started_at, ended_at (nullable), sort_order | One active chapter per user (ended_at IS NULL). Archived chapters are immutable. Existing data is implicitly Chapter 1. See ADR 0007 |
| `reflections` | id, user_id, chapter_id, cycle_type, cycle_start, cycle_end, expectation_text, observation_text, intention_text, did_tune, body_text, prompt_text, created_at, updated_at | Cycle-close ceremony entries + free-form journal entries. cycle_type in (week, month, quarter, year, free). Chapter-scoped. intention_text is the forward-facing prompt from Move 2 (behavior change layer). See ADR 0007 |

## Current state of the build

**As of this session (May 2026), the following is working:**

- Auth (login / signup / sign out) — verified working on mobile (iOS)
- Onboarding flow (noun-shaped swell menu → per-swell motions → personalize) — verified working on web. 10 seeded swells (Movement, Mind, Nutrition, Home, Family, Friends, Work, Money, Creativity, Adventure) with descriptions; pick-then-add menu; per-swell motion sections with verb hints; theme + tracking mode + haptics + celebrations on the personalize screen with skip-to-defaults
- Dashboard: daily checklist with flat mode and groups mode, filter toolbar (eye icon + popover: show completed / show swells / show pts), daily progress bar, wave detection
- Motions: create, edit (name/pts/hrs), delete, log, unlog, drag-to-reorder, hide/unhide
- Dashboard interaction model: tap checkbox to log/unlog, tap row body to open detail sheet, grip icon (3×3 dot grid) is drag handle. Swells page uses matching grip icon. Transparent catch overlay during active drag prevents stray taps
- Motion detail sheet: edit name/pts/hrs, two-tap delete, swells chip assignment with contribution weights, groups chip assignment, submotions, unlog, hide toggle
- Submotions: create (via detail sheet), log/unlog independently; shown only in detail sheet (not checklist); budget auto-divides parent's pts/hrs equally across submotions on add/remove/parent edit *(decision May 2026: feature will be hidden behind a `SUBMOTIONS_ENABLED` constant — schema and code stay intact, UI surfaces gated; turn back on later if needed without rework)*
- Search: filter motions by name in flat mode checklist
- Buckets (formerly Groups): create, edit, delete, reorder (drag), enable/disable via settings toggle — user-facing vocabulary is "buckets" per ADR 0012; schema table names stay `groups`/`motion_groups`
- Swells: create, edit (name/color/weekly target), delete, assign motions via toggle chips with per-swell contribution weight (1–100%), weekly progress bars with weighted aggregation, "last week" reference stat, checkmark on target hit, hide-done uses weekly completion
- Celebration trigger: wave/bloom animation fires the moment a motion log crosses a swell's weekly target
- Many-to-many: motions ↔ swells (with contribution_weight), motions ↔ groups
- Log (reports) page: period filter, stats, swells breakdown, daily chart, activity feed, waves
- Settings: theme picker, groups toggle, daily goal, celebration/haptic toggles
- Wave mode: 72hr auto-detection, manual wave return with WaveGrid check-in
- Data export: JSON download of all user data
- PWA: manifest, icons
- Themes: Biarritz (default), Tjørnuvík, Bolinas
- Bottom nav: Motions / Swells / Reflections / Settings, persistent across app pages
- Parallelized SSR data fetching: dashboard, log, and settings pages collapsed from 6–9 sequential DB round trips to 2–4 via `Promise.all`. Wave detection extracted into a helper so its two-query subchain runs in parallel with the rest of the dashboard load
- "Motions not feeding any swell" diagnostic surface on the swells page: quiet section labeled "Not feeding any swell" with subhead "Adopt them into one, or let them go." Each row offers inline Add to a swell (opens detail sheet) and Hide (optimistic). Section hides entirely when there are no orphan motions
- Group-centric assignment: tapping a group in Settings shows two tappable lists (Motions, Swells) under the name/color form. Items already in another group show "in [other]" and reassign on tap. Optimistic updates via existing `setMotionGroup` / `setSwellGroup`
- Onboarding archetype starter packs: 2×2 grid above the swell list (The Maker / The Athlete / The Wanderer / The Scholar — Caretaker dropped pending naming pre-launch). Tap a pack to pre-pick its three swells; tap the active pack again to clear. Picked swells sort to the top of the list so selections cluster together. Active pack highlight clears when user modifies picks. Each theme palette bumped to 10 colors; onboarding seeds the 10 swells from a shuffled palette so each gets a distinct color
- Per-swell proficiency view (`/swells/[id]`): header (color dot + name + this-week tide bar + lifetime stat line "N pts · M weeks running") + Motions section with constellation/list toggle + Milestones section. Constellation = swell node at center showing value/target for the active time window, motion nodes around it sized by points earned in that window (loudest = largest), stroke and text opacity encode recent activity (week > month > lifetime > none), earned value (points or hours) inside each node so the nodes visually sum to the center, motion name label below; tooltip surfaces both value and log count, cap-8 with "+ N more" overflow to list view. Time-view toggle (Week / Month / Lifetime) drives both per-node values and the center text: Week = weekly value/target, Month = calendar-month value / `ceil(weekly_target × days_in_month / 7)`, Lifetime = lifetime value / `ceil(weekly_target × weeks_since_first_log)` (falls back to absolute lifetime total when `weeks_since_first_log ≤ 1` — ADR 0005). Milestones support recurring (weekly/monthly cadence) and one-shots (complete/uncomplete toggle); completed one-shots collapse under "N finished milestones ↓"; names are inline-editable. The list-row swell on /swells navigates here; inline-expand is gone. Schema: new `milestones` table. Drag-to-arrange was tried and dropped — labels + size-by-points read better; `motion_swells.position_x/y` columns were added and then removed in the same session
- Settings build picker (`/settings/shape`): "Your shape" sub-route surfaced from a row on the Settings page (sits right under Appearance, summarizing the current shape). Two slots — primary and secondary — over a 2×2 preset gallery (The Maker / The Athlete / The Wanderer / The Scholar). Both slots are optional and NULL by default; secondary is the multi-classing entry (slot only in v1, blending math is deferred to the Logs redesign). Tapping a preset opens a preview/diff listing the preset's seeded swells with "have"/"new" badges and per-row opt-out checkboxes, then two CTAs ("Set as primary" / "Set as secondary"). Confirming creates only the opted-in new swells with theme-palette colors and weekly target defaults matching the user's tracking mode, never touching existing swells. Clearing a slot nulls the preference without touching any data. Schema: `user_settings.primary_build`, `user_settings.secondary_build` (both nullable text). Preset definitions live in `lib/builds.ts`; onboarding's archetype packs now read from the same shared source of truth
- Toggle-active contrast pairing: active segmented buttons use `bg-th-text` + `text-th-bg` so the inversion works in light and dark themes. The old pattern (`text-th-btn-text` = always white) broke in any theme where `--th-text` is light
- Normalized contribution model (ADR 0003): a motion's `contribution_weight` across its swells sums to ≤100%. Toggling a swell on uses the remaining capacity as the default (no more silent 100%/100% double-counting). Editing one swell's % auto-rebalances the other swells on the same motion proportionally so the total stays unchanged. Motion detail sheet shows a quiet "X% allocated · Y% remaining" indicator next to the Swells label. `setMotionSwells` normalizes server-side as a safety net. One-time renormalization SQL ran 2026-05-17 — two motions (Meditate, Spend time outside) were at 100/100 across two swells, both became 50/50. Shared helpers live in `lib/contributions.ts`
- Logs radar (`/log`, floating above the existing report sections — no panel chrome): N-gon of pie-slice wedges (one per swell, ordered by `sort_order`, starting top and going clockwise), each in its swell color at 0.34 fill opacity. On top of each wedge, a filled "slice" in the same color at 0.65 opacity scales radially with this week's actual value — reads like a fuel gauge inside the wedge. Wedge + slice share boundary geometry on the bisector radials at `axis_angle ± π/N`; wedge corners sit at the chord-bisector intersection `2 R_a R_b cos(π/N) / (R_a + R_b)`, which reduces to the regular-kite case when adjacent targets are equal. A 3N-vertex shoulder polygon strokes the perimeter — the "dive" between unequal neighbors stays on the boundary radials so a low-axis wedge never visually empties out a high-axis neighbor (the v1 bug). Each target vertex is drag-handleable (radial-axis projection, ghost dot + dashed leader, live drag pill bottom-left); persists via a focused `updateSwellTarget(id, currency, value)` server action. Reset-to-build chip bottom-right opens a per-row opt-out dialog. Multi-class chip above the chart reads "{primary} + {secondary}" (single-build users don't see it). Wave-week wash + ramp pill render when the current week intersects a `wave_checkins` row (ramp wiring deferred to the welcome-back session — defaults to 100%). Pure helpers in `lib/radar.ts`, component at `app/components/SwellRadar.tsx`. Subtitle copy follows the period filter ("Your swells this week / this month / over time"); both the radar and the report sections respond to the filter as of ADR 0005. Hide-hidden filter and per-wedge long-press menu deferred per spec (gated until `swells.hidden` ships)
- Swells page weekly aggregate softened (header total at `text-sm`, progress bar 3px tall on `bg-th-border`, denominator readout at `text-[10px]`): the teaching denominator stays visible but no longer reads as the loudest stat on the page now that the Logs radar surfaces the same teaching signal more legibly
- Period-aware Logs radar + calendar-month math (ADR 0005): the radar now honors the `/log` period filter. `This week` keeps the existing Sunday-anchored weekly view. `This month` anchors to the calendar month (1st-of-month → today) with targets derived at render via `ceil(weekly × days_in_month / 7)`, month-to-date actuals, and a `Wave month` pill when there's a 7+ consecutive zero-log day streak inside the month. `All time` is read-only — drag disabled, static handles — with targets = `ceil(weekly × weeks_since_first_log)` and fallback to absolute totals when `weeks_since_first_log ≤ 1`. Drag still edits the weekly source-of-truth from any view (monthly drag converts display×7/days_in_month back to weekly with weekly-step rounding); the live pill on monthly reads `{Swell} · 222 pts/mo · 50 pts/wk`. Reset-to-build diff dialog mirrors the same `monthly (weekly)` format on month view. Filter labels: `This week / This month / All time`. Proficiency view's Month tab switched from rolling-4-weeks to calendar-month using the same helpers. Pure helpers in `lib/periods.ts` (daysInMonth, monthStartKey, weeksSinceFirstLog, consecutiveZeroDayStreak, monthlyTargetDisplay, lifetimeTargetDisplay, ceilDisplay, pacificDayKey, dayKeyRange)
- Ceil display rule applied (ADR 0005 §6): every visible target / actual / percentage is `ceil`'d for display — log page totals/avg/swell breakdown/daily chart rows/recent entries, radar drag pill + reset dialog, proficiency view formatValue + center text + constellation node values, swells page weekly aggregate (header + progress percentage), per-swell row weekly progress, daily checklist progress percentage. Points mode rounds up to whole integers; **hours mode rounds up to 0.25 hr and displays as HH:MM** (e.g. 0:30 not "0.5 hr") — decimal-hours display is banned. `ceilDisplay`, `monthlyTargetDisplay`, and `lifetimeTargetDisplay` in `lib/periods.ts` take an `isHours` flag. Polygon geometry still runs on unrounded floats so vertices land in the right pixel; only the displayed numbers round. Allocation-percentage displays in the motion detail sheet stay on `Math.round` — proportional inventory that must sum to 100, not celebration metrics
- Waypoints rename (ADR 0006): user-facing "Milestone(s)" → "Waypoint(s)" on the per-swell proficiency view — section header, add aria-label, empty state, finished-collapse count. Schema (`milestones` table), action file (`app/actions/milestones.ts`), component (`MilestonesSection.tsx`), and type (`Milestone`) names stay per the internal/external split already in use for build/shape. Empty-state copy also dropped "goal" (on the never-use list); now reads "No waypoints yet. Add one to mark something you're navigating toward."
- Export translates to user vocabulary (ADR 0006): `/api/export` now includes waypoints (previously missing entirely — real data-loss surface for backup), and translates top-level keys to user-facing words: `wave_checkins` → `waves`, new `waypoints` array sourced from `milestones`. Motion→swell contributions nest inside each motion as `swells: [{ swell_id, weight }]` rather than a top-level `motion_swells` junction — reads as "this motion feeds these swells with these weights" instead of a junction-table dump. Foreign-key field names (`swell_id`, `motion_id`) stay as join handles. Filename / `exported_at` / `user_email` unchanged
- **E2EE-aware export + full restore, all chapters (content-encryption Phase 4/5, 2026-06-18):** Export moved **client-side** (`app/settings/ExportButton.tsx` + `getExportData`) so it decrypts content in the browser into a plaintext, restorable backup — required because once migration flips on, the server only ever sees ciphertext and a server-built export would be unreadable. Format is **v3** (`lib/export-format.ts`): it covers **all chapters, not just the active one** (a `chapters` array + a `chapter_id` on every chapter-scoped entity — swells/buckets/motions/logs/waves/anchors), and adds **anchors** (the journal text), the most irreplaceable content under E2EE. The old server `/api/export` route was **deleted** (superseding the ADR 0006 export entry above — it would have exported ciphertext). **Restore-from-export** (`RestoreFlow` at `/settings/import`, below the AI import) reads a backup file and rebuilds the user's **full history** — structure, motion↔swell junctions, logs (with `logged_at` + intensity preserved), waypoints, waves, anchors, and **archived chapters** (recreated with their original started/ended dates). Same operator-blind discipline as the AI import: `lib/restore-resolve.ts` resolves the export's old-id graph to index / `ChapterRef` references **client-side** and re-encrypts names/text in the browser; the `restoreExport` server action inserts blindly, correlating new rows by `sort_order` (not names or RETURNING order) and routing each row to its chapter (the export's active chapter → the user's current active chapter; archived → freshly recreated). Dedup-by-name applies **only within the active chapter**; archived-chapter entities are always recreated. Clear options (add alongside / archive-and-restore-fresh / replace) mirror the AI import. Motivation: under E2EE a lost key means permanently lost data, so a user-held plaintext backup + restore is the safety net. Resolvers are unit-tested (`lib/import-resolve.test.ts`, `lib/restore-resolve.test.ts`), the import resolution path is validated on a real AI export, and the **encrypted-on round-trip (export→restore with `enc_enabled = true`) was verified end-to-end 2026-06-18** — the last unproven path in the safety net is now confirmed. **Junction-export bug (found + fixed 2026-06-18 via a real restore test):** `getExportData` queried `motion_swells` with `.eq('user_id', uid)`, but that table has **no `user_id` column** (it's scoped by the `own motion_swells` RLS policy through the parent motion). The filter errors against a missing column and PostgREST silently returns **zero rows**, so every export captured motions/swells but **none of their assignments** — restored motions came back unassigned. The bug was inherited verbatim from the old `/api/export` route, meaning assignments were never actually in any backup. Fixed by dropping the filter and relying on RLS. Lesson: a Supabase query against a non-existent column returns empty instead of throwing. The export assembly is now unit-tested (`lib/export-build.test.ts`, guarding the junction-drop symptom), and the **live encrypted-on round-trip — including assignments — was verified 2026-06-18**.
- **Passkey as login + content key, one ceremony (2026-06-18):** the passkey now authenticates the account AND unlocks content encryption in a single Face ID. Uses Supabase's **experimental passkey auth** (beta — `experimental: { passkey: true }` on `lib/supabase/client.ts`, requires `@supabase/supabase-js` ≥ 2.105) via its **two-step API** (`startRegistration`/`startAuthentication` → run the ceremony ourselves → `verify*`), so we can add the WebAuthn **PRF** extension to the same ceremony Supabase otherwise controls. Verified end-to-end against the beta with a throwaway spike: `verifyRegistration`/`verifyAuthentication` issue a valid session even with prf on the ceremony, and the PRF secret is byte-stable across register↔login. New `lib/auth/passkey.ts` (`registerPasskey` / `authenticatePasskey` — Supabase-routed ceremonies, serialization, constant salt); `KeySetup` now registers through Supabase (the passkey becomes a real login credential, not the old local-only key source); `/login` gains a primary "Sign in with Face ID / passkey" button that authenticates + unwraps the DEK + sets it, with **email+password as the fallback login** and recovery-code/password as the fallback content unlocks. Old local-ceremony `registerPasskeySlot` removed from `lib/crypto/keys.ts`. **Design change: the PRF salt is now a fixed app-level constant** (`onduler-content-key-v1`), not per-credential — discoverable sign-in doesn't know the credential up front, so a constant salt lets login derive the same secret in one ceremony (safe: PRF entropy is the authenticator's internal secret). The earlier-observed sign-in "flakiness" was stale leftover credentials from spike testing confusing the discoverable picker, not a real bug — production only ever creates Supabase-registered passkeys, so it doesn't recur. **Requires a manual Supabase dashboard change (not in repo): Authentication → Passkeys → enable + set Relying Party ID = `onduler.app` and RP Origins = `https://onduler.app`. The RP ID is FROZEN once users enroll passkeys (changing it bricks them all) — set it before real signups. For local dev, RP ID = `localhost` (passkeys are domain-bound, so a passkey enrolled on localhost won't work on onduler.app and vice-versa).** Cross-device (iPhone↔Mac iCloud) wasn't re-exercised here but the original PRF spike proved the secret syncs byte-identical and Supabase passkeys are standard iCloud-synced passkeys — confirm on `onduler.app` when convenient.
- Motion-side cadence + waypoint completion loop (ADR 0004 §7): a Cadence section on the motion detail sheet (`app/dashboard/components/CadenceSection.tsx`) lets the user set a recurring cadence (weekly/monthly × N, optional bonus points) directly on a motion. Save creates/updates a recurring waypoint on the motion's primary swell (highest contribution_weight, with a picker when the motion feeds multiple). Schema additions: `milestones.motion_id` nullable FK + new `milestone_hits` event-log table (one row per cycle hit). `quickLogMotion` runs a post-hook that detects linked recurring waypoints crossing target_count and inserts hit rows; the latest hit's Pacific day key gates re-firing within the same cycle. Bonus points accrue to the parent swell's totals (week / month / lifetime) via `lib/waypoints.ts.bonusBySwell` across the dashboard, log page, swells page, and proficiency view — points mode only for v1 (the `bonus_points int` column doesn't map cleanly to 0.25-hr precision). The existing dashboard celebration trigger fires when bonus pushes a swell over its weekly target. The proficiency view's Waypoints section now renders a partial-ring progress visual on recurring rows (SVG arc, `stroke-dasharray` based on `currentCycleCount / targetCount`) and exposes a "Mark" button on unlinked recurring waypoints for the explicit-tap path. Pure cycle helpers in `lib/cadence.ts` (cycleStartKey, logsInCycle, hasHitInCycle, cycleProgress)
- Wave / build interaction (ADR 0004 §8): WaveGrid check-in now routes to a welcome-back screen at `/wave/return/welcome` instead of straight back to the dashboard. Two cards — Ease back in (recommended) and Pick up your [shape] — plus a quiet "Try a different shape →" link. A subtitle line surfaces auto-resolved MVS anchors ("Still showing up: {motion 1} · {motion 2}") so the user sees what's still them when they return. Picking *Ease back in* sets `user_settings.welcome_back_mode = 'ease'` + `welcome_back_started_at = now()`; the radar wash + ramp pill go live for 3 weeks (40% → 70% → 100%) via `lib/welcomeback.currentRamp`, and the dashboard's celebration-trigger swell targets soften by the same ramp so celebration stays reachable. *Pick up your [shape]* records the choice but applies no ramp. Ramp auto-clears after week 3 by virtue of `currentRamp` returning null; stale state has no effect. Settings → Your shape gains an Anchors editor under the primary slot: drop/swap/add up to 4 motions from the eligible pool (motions feeding the primary shape's seeded swells), persisted in `user_settings.mvs_anchors jsonb` keyed by build key; "Reset to defaults" clears the override so the auto-top-2 reapplies. Pair fixes: editable waypoint `kind` flip (small ↔ button next to delete; `updateMilestone` accepts `kind` and clears the inactive kind's fields server-side); all-HAVE empty state in the build preview ("All N already in your set — set as primary" replaces the opt-out grid when every preset swell already exists); constellation node-label opacity floored at 0.6 + label color bumped from `--th-muted` to `--th-secondary` so quiet motions stay legible in dark mode
- Hidden swells: `swells.hidden bool DEFAULT false` lets the user hide a swell when focus shifts without losing logs, motions, or contributions. Hide control sits next to the back link on the per-swell proficiency view (small "Hide" / "Restore" text button). Hidden swells drop from the dashboard celebration trigger, the `/log` radar + by-swell breakdown, the `/swells` main list, and from `motion.swells` junctions client-side — so motions feeding only hidden swells correctly fall into the "Not feeding any swell" orphan diagnostic. A quiet "N hidden swell(s) ↓" expander on `/swells` lists them with a Restore button; rows link to the proficiency view so the user can review before restoring. Hidden swells continue to receive waypoint bonus accrual silently (the data path is unchanged) — the visual is what's gated. Export, build preview "already have" detection, and the proficiency view (deep-linkable) all see hidden swells normally so data integrity stays intact
- Reflections surface — Sessions A+B+C (ADR 0007): rename Today → Motions (with a checkbox icon) and Log → Reflections (mirror-shaped icon). Hard route rename `/log` → `/reflections`; every `revalidatePath` call updated. New `chapters` (one active per user; backfilled Chapter 1 for existing users with `started_at = MIN(logs.logged_at)`) and `reflections` (chapter-scoped; supports both ceremony entries and free-form) tables. Weekly cycle-close ceremony at `/reflections/ceremony/week`: 3-step flow — *"What did you expect to see this week?"* → frozen-radar reveal of last week's actuals → *"What did you see?"* → *"Want to tune something?"* CTA (Swells / Motions / Skip). Every step skippable; completing or fully skipping writes a reflection row, abandoning leaves no trace (indicator just expires on next cycle rollover — no stacking, no guilt). Pending-week invitation banner on `/reflections`; BottomNav tide-pulses the Reflections icon + slightly dims other tabs when a ceremony awaits. First-time-only engagement-floor unlocks per cadence — `UNLOCK_FLOOR { week:3, month:8, quarter:25, year:80 }` distinct log days inside a closed cycle; `CEREMONY_FLOOR { *:1 }` for subsequent cycles after first unlock. `getUnlockState` returns `{ week, month, quarter, year }` booleans per chapter; once unlocked, stays unlocked for the chapter. When `!unlocks.week`, `/reflections` renders a full vibe-only `LockedPage` (blurred hex silhouette + drifting tide lines + soft copy, no radar, no period filter, no banner). When weekly is unlocked but later cadences aren't, a "Coming together" tile stack shows blurred-silhouette anticipation cards for each not-yet-unlocked cadence; tiles disappear individually as their cadence unlocks. Calendar-anchored cycles (Sun→Sat for week; 1st of month; calendar quarter Q1=Jan–Mar etc.; calendar year). Pure helpers in `lib/cycles.ts` (closedWeekFor/closedMonthFor/closedQuarterFor/closedYearFor + label formatters + `cycleContaining(day, cadence)` for bucketing logs) and `lib/unlocks.ts` (`logDaysInCycle`, `unlockedForCadence`, the floor maps). Monthly/quarterly/yearly cycle-close ceremony routes (the actual `/reflections/ceremony/month` etc. surfaces) are deferred — for now an unlocked non-weekly cadence just removes its tile and waits for its ceremony surface to ship
- Anchors rename (ADR 0008): Reflections → Anchors across surface name, entry-level word ("drop an anchor"), route (`/reflections` → `/anchors`), file moves (`app/reflections/` → `app/anchors/`), schema column rename (`user_settings.mvs_anchors` → `mvs_motions`). Internal `reflections` table preserved per the internal/external split. Two small additions folded in same session: italicized tertiary line "Every motion leaves a wake." on `LockedPage.tsx`, and the hex `slow-breathe` keyframe widened (opacity 0.85↔1 → 0.55↔1, plus a `scale(0.96)↔scale(1.04)` transform with inline `transformOrigin: center`) so the breath now visibly registers on iOS. Identifier renames swept for grep-ability: `setMvsAnchors`/`defaultMvsAnchors`/`resolveMvsAnchors` → `*Motions`; `anchorsOverride`/`anchorPool`/`resolvedAnchorIds` → `mvs*`; `AnchorsEditor` → `StillShowingUpEditor`; `pendingReflection` prop → `pendingAnchor`. Settings → Your shape "Anchors editor" section renamed to "Still showing up" to free the word for the surface
- Polish batch (testing-notes 2026-05-20): search bar removed from the Motions sticky header (the by-swell view mode supersedes it for finding motions); Settings hides the group rows under the toggle when groups are off; Default-theme dark background flipped to true `#000` (Bolinas/Biarritz dark stay theme-tinted); bottom-nav canonicalised — inner content row locked to a fixed `h-14`, safe-area-inset applied as `padding-bottom: max(env(safe-area-inset-bottom), 0px)` on the `<nav>` itself (a first attempt using a sibling spacer `<div>` caused per-page jitter on iOS PWA — collapsed back to padding on the nav so iOS treats it as a single box); body padding switched to `pb-[calc(3.5rem+env(safe-area-inset-bottom))]` to track exactly, and body `min-h-full` → `min-h-[100dvh]` so the layout doesn't depend on the html-percentage chain when iOS chrome state shifts between route transitions; subtle `border-b border-th-border-soft` on motion rows for the paper-list aesthetic; `+` button in the Motions section of the per-swell proficiency view opens the existing keyboard-takes-over add-motion form scoped to that swell (new `swellId`/`swellLabel` props on `AddMotionForm`; `createMotion` now reads `swell_id` from formData and inserts a `motion_swells` row at 100% contribution); drag activator moved off the dotted left-handle glyph onto the motion checkbox itself (`touchAction: 'none'` + `cursor-grab` on the checkbox, glyph removed); third view mode "By swell" on the dashboard (per-swell sections with each motion repeated under every swell it feeds, plus an Unassigned section for orphans; tap any instance to log once — credit still distributes per existing `contribution_weight`; new `StaticMotionRow` for non-draggable rendering; `bySwell` toggle persisted in `localStorage`)
- Polish batch (2026-05-21): font assignment flipped (Manrope → headers, IBM Plex Mono → body); motion-row checkbox is the drag activator (long-press), and tapping anywhere else on the row still logs and shows the done visual (checkmark, line-through, opacity drop) — initial pass stripped the done visual but Josh wanted it back; the dashboard By-swell view now supports drag-to-reorder via per-swell `DndContext`s that write back to the global `motions.sort_order` by filling only the section's slots in the global list (other sections keep their positions); the `/swells` search input was removed (the by-swell view + group filter cover the same need); the swells list drag-handle glyph is now a 20×20 checkbox shape that matches the motion rows and auto-fills with a check mark when the swell's weekly target is hit; motions-header layout split into two rows — `By swell` / `Default view` on the left and `Hide done` / `Show all` on the right of a single toggle row, with group filter chips moved to their own row underneath (chips now also appear in by-swell mode and filter the swell sections by `activeGroup`). `StaticMotionRow` deleted (no remaining callers).
- Anchors D (2026-05-21): persistent anchors journal at `/anchors/journal` — chapter-grouped chronological library (newest chapter on top) with two card shapes. Ceremony anchors render the real `FrozenRadar` re-aggregated from current logs + `motion_swells` weights across the cycle window (post-Anchors-E: scoped to each anchor's chapter, so past-chapter anchors render against their own chapter's swells). Free anchors show optional prompt header + body. `+` button in the top-right of `/anchors` and a Journal link sit alongside the desktop-only back link. New routes: `/anchors/new` (keyboard-takes-over create form with optional themed prompt picker drawn from `lib/anchorPrompts.ts` — placeholder bank organized by notice / honor / consider / release / invite; final wording pending) and `/anchors/[id]/edit` (body + prompt + cycle chip picker — This week / Last week / No cycle, plus an "Original" chip when the saved cycle is some other week — and a two-tap delete control). Server actions: `createFreeAnchor`, `updateFreeAnchor`, `deleteFreeAnchor` (the latter two gated to `cycle_type='free'` only — ceremony anchors are frozen records). `getAnchorJournal()` returns chapters grouped with their anchors. No schema change — `reflections` already supported `cycle_type='free'` + `body_text` + `prompt_text`
- Anchors F (2026-05-21): monthly / quarterly / yearly cycle-close ceremony routes. All four cadences now share a single dynamic route `/anchors/ceremony/[cadence]/page.tsx` — the old static `/anchors/ceremony/week/page.tsx` is gone; `WeekCeremony` lifted into a shared `CycleCeremony` with cadence-pivoted copy (expectation / reveal / observation prompts swap the cycle noun: "week" / "month" / "quarter" / "year"). `FrozenRadar` moved up to `app/anchors/ceremony/FrozenRadar.tsx` so all cadences share it. Server actions generalized: `getWeekCeremonyState`/`saveWeekReflection`/`fetchWeekCeremonyPending` → `getCeremonyState(cadence)`/`saveReflection`/`fetchAnyCeremonyPending`. Layout bottom-nav tide-pulse now fires for any pending cadence (resolves all four cadences in one round-trip). `/anchors` header swaps the single weekly banner for a per-cadence stack — one "ready to be seen" tile per pending cadence (week / month / quarter / year order). Targets scale to the cycle window via `daysInCycle × weeklyTarget / 7` for non-weekly cadences (weekly stays 1× — weekly is the source of truth per ADR 0005). New `lib/cycles.ts` helpers: `daysInCycle`, `closedCycleFor(today, cadence)`, `formatCycleLabel(cycle, cadence)` dispatchers. Logs query uses a generous timestamp bound + precise Pacific-day-key filter in JS so we don't need PT-midnight Date helpers per cadence
- Anchors E (2026-05-21, ADR 0009): archive-and-fresh-start ships. **Schema**: `chapter_id` NOT NULL FK added to `motions`, `swells`, `groups`, `logs`, `wave_checkins`; backfilled to each user's active chapter; composite `(user_id, chapter_id)` indexes on all five. Migration script at `scripts/migrate-anchors-e-chapter-fk.sql` (wrapped in a transaction; idempotent; the canonical `supabase-migration.sql` is updated to reflect the new shape with chapters hoisted above the entity tables). Other tables inherit chapter ownership transitively (`motion_swells` via motion, `milestones` via swell, `milestone_hits` via milestone, `reflections` already chapter-scoped per ADR 0007). **Active-chapter helper**: `lib/chapters.ts → getActiveChapterId(supabase, userId)` lazily creates a chapter if missing — handles new-signup gracefully. **Query sweep**: every active-surface read of the entity tables now scopes by `.eq('chapter_id', chapterId)`; every entity insert sets `chapter_id`. ~86 queries across 19 files. **Archive action**: `app/actions/chapters.ts → archiveAndStartFreshChapter()` ends the active chapter (`ended_at = now()`), inserts a new one, resets `onboarding_complete` + `primary_build` + `secondary_build` + `mvs_motions` + `welcome_back_mode` + `welcome_back_started_at` on `user_settings` (theme / tracking mode / daily goal / haptics / celebration stay — those are user-level preferences). `revalidatePath('/', 'layout')` clears the whole route tree. **UI**: a quiet "Start a new chapter" link sits at the bottom of the `CycleCeremony` tune step (below Swells / Motions / Skip); tapping routes to an in-flow `archive_confirm` step ("Close this chapter?") with a soft warning ("take it as a gift to your future self, not a goodbye"), then a single confirm button that saves the ceremony reflection, runs the archive, and sends the user to `/onboarding`. **Journal**: past-chapter ceremony anchors render against their own chapter's swells (per-chapter grouping at fetch time), not today's — past chapters keep their visual identity. **Export**: scopes to active chapter only; past-chapter export is a future surface. **Deferred**: Settings → Past chapters browse (data is preserved end-to-end; the read-only surface is its own session). User-facing surf vocabulary still six terms — chapter is an internal/external split (the user sees "chapter" in the archive UI but the surf family — Tide / Wave / Swell / Motion / Waypoint / Anchor — is unchanged)
- Anchors E migration applied to remote DB (2026-05-21, late): `scripts/migrate-anchors-e-chapter-fk.sql` had only made it onto `reflections` on the live Supabase project — `motions` / `swells` / `groups` / `logs` / `wave_checkins` were still pre-Anchors-E. The whole post-shipping `chapter_id` query sweep had been silently 400ing in prod, which read as "everything got wiped" to the user. Ran the script (idempotent, transactional): backfilled every existing row to the user's active chapter, then locked the column NOT NULL with the composite `(user_id, chapter_id)` indexes. Once-only gap, not expected to recur — calling it out because the PROJECT.md entry for Anchors E above describes intended-state and the live DB only matched it as of tonight
- Radar polish (2026-05-21): five fixes in one batch on the Logs radar. (1) `chartCeiling` now scales tight to data instead of locking at `initialCeiling` — low-target users no longer see a pinhole polygon in an oversized frame. (2) SIZE 320→360, RADIUS 108→130, label font 10→13, `max-w-[320px]` cap removed, svg `overflow: visible` so labels can bleed past the viewBox without clipping. (3) Lifetime view's static target handle switched from a white-fill / blue-stroke marker to a small swell-colored dot at 0.7 opacity — the original was reading as "this swell is highlighted" when a large actual visually swallowed the marker. (4) `shoulderVertex` now uses the same chord-clamp rule as `slicePath` chord ends (`min(actualR, wedgeBoundaryR)` along the bisector instead of `actualR × cos(π/N)`), so the polygon outline and slice fill align on the bisector under target — the previous gap let slice fills bleed past the outline. Shoulder positions take `targets` now, and `actualPolygonPath` does too; both callers (SwellRadar + FrozenRadar) updated. (5) Drag damping: `DragState` snapshots `chartMax` at drag start so the pointer-to-value math doesn't feed back on itself (was causing exponential runaway — each frame's growing value pushed chartMax up which made the next frame's pointer-at-same-position map to an even bigger value); past 2× snapshot, each pixel contributes 35% as much value, so dragging a small swell way above the others slows down progressively instead of rocketing to hardCeiling. Also folded in: Anchors page header drops the Journal link + circled-`+` (uses the bare large `+` from Motions/Swells); back-button removed from Settings / Swells / Anchors landing pages since bottom nav handles navigation
- Past chapters browse (2026-05-21): Settings → Past chapters surface. `/settings/chapters` lists archived chapters newest-first by `sort_order` with date range + duration + log count per row. `/settings/chapters/[id]` is the read-only detail — chapter date range header, stat line (`N logs · N motions · N swells · N anchors`), swells section (color dot + name + weekly target chip), motions section (name + default pts/hrs chip), and an "N anchors in this chapter →" tile that bounces to the existing journal (the journal already groups by chapter and renders `FrozenRadar` per anchor against the *anchor's* chapter shape — duplicating that here would have been heavy and the journal owns it well). Entry point: a "Past chapters" row right under Your shape in Settings, conditional on `archivedChapterCount > 0`. New server actions in `app/actions/chapters.ts`: `listArchivedChapters()` and `getArchivedChapterDetail(id)` — the latter returns null and triggers `notFound()` if the chapter is the active one, doesn't exist, or isn't owned by the user. Settings page count query joins the parallel `Promise.all` so the conditional row adds zero round-trips
- Submotions toggle + per-parent Split/Full mode (2026-05-21): Settings → Tracking gains a Submotions toggle (default OFF; mirrors the Groups toggle pattern) — when ON, the Motion detail sheet surfaces the Submotions section gated by a per-user `user_settings.submotions_enabled` bool instead of the old `SUBMOTIONS_ENABLED` constant (which is gone; `lib/features.ts` deleted). New `motions.submotion_mode text NULL` per-parent column with CHECK in ('distribute', 'rollup'); NULL treated as distribute. First-child add form shows two small chips ("Split" / "Full") above the name input — Split is the existing auto-divide (parent's pts/hrs evenly across children), Full means each child carries the parent's full pts/hrs. After the first child exists, a small Split/Full chip in the section header toggles mode on existing parents (triggers server-side rebalance/refill via new `setSubmotionMode` action). `createSubmotion(parentId, name, mode?)` stamps the parent's mode on first child. `rebalanceSubmotionBudget` became mode-aware. New submotions default their swell chips to the parent's swells (with the parent's contribution weights) so the user doesn't re-pick on every add. Migration: `scripts/migrate-submotions-mode.sql` (idempotent, transactional)
- Inline swell editing on per-swell proficiency view (2026-05-21): name (tap `<h1>` → text input → blur or Enter saves), color (color dot is a tappable `<input type="color">` → autosave), weekly target (tap denominator → number input → blur saves; respects tracking mode — 1-step pts vs 0.25-step hrs; when target is null, a quiet `+ target` button replaces it), group chips (chip row under header when groupsEnabled + groups exist, optimistic toggle via `setSwellGroup`), and Delete swell (two-tap confirm above an `mt-12 border-t pt-4` block at the bottom, routes to `/swells` on confirm). All edit paths funnel through `updateSwellDirect`. Render-phase sync pattern (the SwellsList ref/check-then-setState technique) keeps `mode` synced when `motion.submotionMode` changes via router.refresh. Enter behavior single-paths through `e.currentTarget.blur()` to avoid the double-commit race the original Enter-then-onBlur path triggered. `/swells/[id]/page.tsx` now fetches `group_id` on the swell + the user's groups so the chip row can render. **SwellRow trimmed**: kebab gone, inline edit form + state gone (`updateSwellDirect`/`deleteSwell`/`setSwellGroup` no longer imported there); rows are now a single tap-to-navigate Link with name + this-week value + progress bar + last-week stat
- Wide-screen mode — sidebar shell at md+ (2026-05-21): new `app/components/SideNav.tsx` — sticky `h-[100dvh] w-60` left rail at md+; vertical route list with icon + label; active row uses `bg-th-surface text-th-text` fill; same Anchors tide-pulse + dot when a ceremony pends. Safe-area padding (`max(env(safe-area-inset-top), 1.5rem)` for top, same for bottom, `max(env(safe-area-inset-left), 0.75rem)` for left) so landscape iPhone PWA clears the dynamic island. Drops the repetitive "Onduler" label since the sidebar is on every page already. BottomNav gets `md:hidden` and both nav components import from a shared `app/components/navItems.tsx` (`NAV_ITEMS`, `NAV_HIDE_ON`, `isHiddenRoute`, `isActiveRoute`) so they never drift. Root layout `body` becomes `flex-col md:flex-row` so the sidebar sits to the left of main at md+; bottom-nav padding (`pb-[calc(3.5rem+env(safe-area-inset-bottom))]`) drops at md+ via `md:pb-0`. Content column flips from centered (`mx-auto max-w-lg`) to left-aligned (`md:mx-0 md:ml-12 md:mr-auto`) so it sits 48px from the sidebar with all the empty space pushed to the right. Mobile (< md) is unchanged. Multi-column per-page layouts (proficiency view, Anchors, Settings) and content-column widening explicitly deferred to future sessions. Hydration-warning footnote: two remaining dnd-kit sortable wrappers (`SwellsList` SortableSwellItem + `MotionDetailSheet` edit-form item) got `suppressHydrationWarning` to match the two that already had it — dnd-kit's `aria-describedby` counter mismatches SSR vs client and the warning is harmless metadata
- Admin diagnostics at `/admin` (2026-05-21): read-only operator surface gated by a new `user_settings.is_admin boolean NOT NULL DEFAULT false`. New `lib/supabase/admin.ts` exposes `createAdminClient()` — service-role Supabase client marked `'server-only'` so a wayward client-bundle import becomes a build error. `lib/admin.ts → requireAdmin()` redirects signed-out users to `/login` and `notFound()`s non-admins so the admin path stays invisible to ordinary users. `/admin/page.tsx` surfaces: total user count, active 7d / 30d (from `auth.users.last_sign_in_at`), system totals (motions / swells / logs / anchors counts via `head: true` count queries), and the most recent 20 signups as tappable rows. `/admin/users/[id]/page.tsx` shows the per-user profile (onboarding state, tracking mode, theme, admin flag, primary/secondary shape), counts (motions / swells / logs / chapters / anchors / waypoints), last log timestamp, and the last 5 logs with motion names. Requires `SUPABASE_SERVICE_ROLE_KEY` in env (local `.env.local` + Vercel Production/Preview env vars — Vercel auto-flags it as sensitive and blocks Development which is fine since local reads from `.env.local`). Schema migration: `scripts/migrate-admin-flag.sql`. Pre-launch ops note: admin was flipped from Josh's personal `jryanjacobs@gmail.com` to a dedicated `ondulertest@gmail.com` so the personal account stays a normal-user testbed; 124 seed-script log rows from `scripts/seed-logs.mjs` were also cleaned out of `jryanjacobs` via the existing `scripts/cleanup-seed-logs.sql` (whole-second timestamp signature; real sub-second app logs untouched). Out of scope for v1: switch-to-user impersonation, any writes / destructive ops, search / filter / pagination beyond the 20-row recent list, charts, BottomNav/SideNav link (path is URL-typed only)
- Design system v1 (`design_handoff_onduler_v1/`): refined theme tokens for Default (deep ocean) / Bolinas (norcal fog) / Biarritz (atlantic surf) × light/dark with new semantics — `--th-surface-2`, `--th-border-soft`, `--th-accent`, `--th-accent-text`, `--th-success`, `--th-wave-wash`, `--th-swell-1..10` — registered in `@theme inline`. `lib/theme-colors.ts` splits palettes into `{ light, dark }`; `getShuffledThemePalette` / `getRandomThemeAccent` take a `ThemeMode`; client callers use a new `detectMode()` helper and `adoptBuild` accepts mode from `ShapePicker`. Typography: Manrope (display, weights 300/400/500/600/700) + IBM Plex Mono (body, weights 400/500/600/700 + real italic at each) — the display/body assignment was flipped 2026-05-21 after testing the original IBM-on-headers reading; `h1/h2/.display` get `+0.05em` tracking, `.kicker` lands the 11px all-caps label, body picks up `ss01` + `tnum` features. Swept `tracking-tight` off all 19 h1/h2 sites since it fought the new positive letter-spacing. Bottom-nav icons refreshed: Swells (compass), Reflections (anchor), Settings (sliders); Motions unchanged. `slicePath` chord-bleed fix in `lib/radar.ts`: chord ends clamp to neighbor wedge boundary radii, peak caps at the swell's own targetR — large actuals next to small-target neighbors no longer leak into the neighbor's wedge; overshoot now reads through the shoulder polygon perimeter, not the per-axis fill. New `app/components/WaveField.tsx`: DPR-aware canvas, 8 stacked sine waves with a gentle second harmonic, each fills below itself with `--th-bg` to mask earlier-drawn waves — the layered-water effect from the handoff. Rewritten `LockedPage` is fully vibe-only: WaveField backdrop + irregular 7-vertex silhouette (blurred wash at filter:blur(7px) + sharp spoked layer breathing on a new `slow-breathe` 4s keyframe), 20px display copy at 0.04em (`Glimpses of where you've been. Come back as you log.`), lowercase 'weekly' cadence label — no h1, no kicker, no engagement counter (ADR 0007 pure-mystery posture). Voice pass: `goals` → `swells` on the wave-return alignment-axis labels; `goal` → `target` on the daily-progress button + Settings → Tracking label. Banned-word audit (tasks / activities / goals / domains) is clean in user-facing copy

- Legal routes shipped (2026-05-23): `/privacy` (Termly HTML, client-rendered via `TermlyContent`), `/cookies` (Termly HTML, same pattern), `/terms` (hand-written JSX, 19 sections), `/contact` (public form → `contact_submissions` table, anon-insert RLS, email fallback). Landing page footer links (Privacy | Terms | Cookies) and signup clickwrap text. `.legal-content` CSS overrides Termly inline styles to match theme tokens
- SSR query parallelization pass (2026-05-23): `getActiveChapterId` moved from sequential to parallel on swells, anchors, settings, and proficiency pages. Swells page collapsed from 3 sequential `Promise.all` batches to 2. Settings page: duplicate `user_settings` query removed. No N+1 patterns found; client-side `SwellRadar` polygon math already properly memoized
- Marketing wake SVG export shipped (2026-05-23): `/api/wake-svg` API route returns standalone print-ready SVG from `generateRandomWake`. Query params: `seed` (default 42), `n` (3–12 swell count, default 7), `size` (200–2400px, default 800), `bg` (background hex, default `#000000`), `stroke` (stroke hex, default `#e8e6e1`), `download` flag adds `Content-Disposition` attachment header with `wake-{seed}-{n}.svg` filename. SVG includes a soft glow layer (gaussian blur + fill at 6% opacity) behind the sharp stroke for depth. Immutable cache headers. Each postcard gets a unique seed so the stack is itself a display of uniqueness. `wakeToSvg` in `lib/wakes.ts` updated to accept `bg` and `stroke` params. `waitlist` and `contact_submissions` tables added to canonical `supabase-migration.sql` (both already live in prod)

- Stripe plumbing shipped (2026-05-23): `stripe` SDK installed. Server-only client at `lib/stripe.ts`. Schema: `stripe_customer_id`, `subscription_status`, `subscription_id`, `current_period_end` added to `user_settings` (migration: `scripts/migrate-stripe-columns.sql`). Webhook endpoint at `/api/webhooks/stripe` handles `checkout.session.completed`, `customer.subscription.updated/deleted`, `invoice.payment_failed` — uses `createAdminClient()` (service role) since webhooks have no user session. Server actions in `app/actions/billing.ts`: `ensureStripeCustomer` (lazy Stripe customer creation), `createCheckoutSession` (monthly/annual/lifetime/custom), `createCustomerPortalSession`. Stripe product **Crew** (`prod_UZbmtDvqLQJACE`) with 4 prices: $5/mo recurring, $36/yr recurring, $99 one-time (lifetime), custom one-time (min $100, preset $150). Statement descriptor: ONDULER SUPPORT. Tax behavior: exclusive. Webhook destination registered in Stripe Dashboard. Env vars (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY`, `NEXT_PUBLIC_APP_URL`, `STRIPE_PRICE_MONTHLY/ANNUAL/LIFETIME/CUSTOM`) set in `.env.local` and Vercel. Billing UI at `/settings/billing` — four plan cards for free users, active/lifetime status for members, Manage button for Stripe portal. Crew row in Settings gated behind `is_admin` for testing. Stripe MCP plugin connected for future dashboard operations

- Polish batch (2026-05-24): bucket edit crash fixed (hooks-order violation — `useState` was after an early return in `SettingsPanel`); calendar icon toggle no longer renders a white background box when active (just highlights to `text-th-text`); eye/filter icon dot indicator removed, icon highlights to `text-th-text` when any filter is active; all six progress bars across the app now use ombré gradients (the two on the anchors page — swell breakdown and daily bars — were still flat fills; now match the `color-mix(in oklch, ...)` pattern used elsewhere)

- Celebrations unified to tideline-only (2026-05-27): all themes now use the same tideline celebration — a swell-colored ombré bar animating via clip-path from the tap point across the row. Per-theme bloom/ripple code removed. `CelebrationState` type simplified (no `type` field). Fires on swell weekly target cross with swell colors + row positioning

- Design handoff audit (2026-05-25): read through `docs/design/handoff-v1/` and verified completeness against the codebase. **Already complete**: bottom-nav icons (§5 — compass, anchor, sliders match exactly), WaveField canvas + LockedPage (§7), Bolinas + Biarritz swell colors (match reference/tokens.css), @theme inline registrations for all semantic + swell tokens, typography rules (.display/.kicker/.tnum/body font-features), voice/copy banned-word audit clean, radar independent wedges (May 25) supersede handoff §6's chord-clamping approach. **Not applied (intentional divergence or deferred)**: Default theme semantic tokens + swell colors differ from handoff (current has monochromatic blue gradient; handoff has diverse 10-color palette), Bolinas dark semantic tokens differ (accent, bg), Biarritz light text + accent differ (#1d4d82 navy vs handoff's #c9521e terracotta), fonts still IBM Plex Mono + Manrope (handoff calls for Courier Prime + Manrope swap), `em` Manrope italic rule not yet added, `--th-frost-overlay` exists but not in handoff

- Bolinas dark earthy brown (2026-05-25): pushed Bolinas dark tokens from blue-gray to earthy brown so it reads as distinct from Tjørnuvík dark. New tokens: `--th-bg: #15120f`, `--th-surface: #1e1a16`, `--th-surface-2: #282420`, `--th-secondary: #b3ada3`, `--th-muted: #897f74`, `--th-faint: #504a42`, `--th-border: #332e28`, `--th-border-soft: #272320`

- Safari auto-zoom fix (2026-05-25): global CSS `input, textarea, select { font-size: 16px; }` prevents Safari from auto-zooming on form focus (sub-16px inputs trigger iOS viewport zoom that doesn't reset on blur, scroll, or navigation — user has to pinch-out manually or force-quit)

- Filter popover backdrop (2026-05-25): transparent fixed backdrop `div` behind the DailyChecklist filter popover captures outside taps and closes the popover — prevents tap-through to motion rows beneath

- LLM-assisted import (2026-05-27): Settings → Import from your AI (`/settings/import`). User copies an Onduler-provided prompt template, pastes it into any LLM alongside life context, then pastes the LLM's markdown output back. Client-side parser (`lib/import-parser.ts`) extracts swells (with targets), motions (with points), groups, and swell/group assignments. Dual-mode parser handles both standard markdown (indented bullets, `#` headers) and rendered copy-paste from chat UIs (no formatting — uses blank-line heuristics for parent/child detection). Preview step shows parsed counts and entity names; confirm triggers `confirmImport` server action that bulk-creates entities following the `adoptBuild()` pattern (dedup by name, palette colors, sequential sort_order, normalized contribution weights). Prompt template at `lib/import-prompt.ts`

- Anchor period filter locking (2026-05-25): `/anchors` period filter options locked to unlocked cadences — month/quarter/year only appear after their ceremony unlocks. URL guard falls back to `week` if the user navigates directly to a locked cadence

- Anchors + Journal v2 (2026-06-01): major overhaul of the anchors surface from spec at `docs/specs/anchors-journal-v2.md`. **Server actions**: `updateFreeAnchor` renamed to `updateAnchor` — now branches on `cycle_type` (free anchors edit body/prompt/cycle, ceremony anchors edit `expectation_text`/`observation_text`). New `updateAnchorField(id, field, value)` for save-on-blur inline editing. New `getAnchorsForPeriod(periodStart, periodEnd, offset, limit)` for paginated period-filtered anchors. New `getJournalData()` returning `JournalChapter[]` with week-bucketed data. **Wave detection**: now counts anchors as engagement alongside logs (parallel queries to both `logs` and `reflections` tables). **Anchors page (`/anchors`)**: Recent section replaced with `InlineAnchorLog` component — period-filtered anchors with save-on-blur editing, 10-per-page load more, expand/collapse per row, two-tap delete for free anchors, ceremony anchors show editable expectation/observation fields. **`/anchors/new`**: cycle chip picker (This week / Last week / No cycle) with hidden inputs. "Your past anchors" link to journal below the form. **Locked page**: `+` button matching all other pages (text-3xl font-light leading-none). **Journal (`/anchors/journal`)**: rewritten as weekly archive. All weeks collapsed by default with expand all/collapse all toggle. Three week renderers: `WaveWeek` (WaveField strip + label), `LogsOnlyWeek` (FrozenRadar from week-level actuals), `AnchorsWeek` (per-anchor cards). **Per-ceremony radar scoping**: ceremony anchors use their own `cycle_start`/`cycle_end` for radar actuals (a month ceremony shows the full month, not just the journal week it was created in). Each ceremony anchor carries its own matched `radarSwells` + `radarActuals` pair. **Historical swell filtering**: swells query now includes `created_at`; radar data for each week/ceremony filters to only swells where `pacificDayKey(created_at) <= cycleEnd`, preventing new swells from appearing retroactively in past radars. **FrozenRadar rewrite**: removed frosted glass wedge fills (were sized by targets, extending past polygon). Now uses polygon-matching per-wedge slices with `vertexAt`/`shoulderVertex` geometry. Separator lines use actuals-based endpoints. **Copy**: "Your wakes this week" → "Your wake this week" (singular)

- Prompt picker accordion (2026-05-25): `/anchors/new` prompt picker changed from a flat dump to an accordion — theme headers are tappable rows, one theme expands at a time, `+`/`−` indicator on each header. Less overwhelming than showing all 25 prompts at once

- Week boundary Sun→Sat (2026-05-25): aligned the entire app to Sun→Sat weekly cycle (Sunday 12:00 AM Pacific start). `lib/timezone.ts`: `mondayOf()` → `sundayOf()`. `lib/cycles.ts`: `thisWeekMonday()` → `thisWeekSunday()`, `closedWeekFor()` returns prior Sun→Sat, `cycleContaining()` builds Sun→Sat. `app/swells/[id]/page.tsx`: `pacificMondayKey()` → `pacificSundayKey()`. Views mode already showed Sun→Sat; everything else was Mon→Sun — now consistent. Ceremonies unlock the moment Sunday hits

- 0% swell allocation visual (2026-05-25): when a swell is added to a motion at 0% contribution (remaining-capacity-as-default when allocation is already at 100%), the chip renders with a dashed border instead of solid fill (`borderStyle: 'dashed', backgroundColor: 'transparent'`). A hint line appears: "Dashed swells are at 0% — edit % to allocate." Applied to both main and submotion swell chips in the motion detail sheet

- FrozenRadar visual update (2026-05-25): the ceremony-reveal radar (`app/anchors/ceremony/FrozenRadar.tsx`) now matches the live `SwellRadar` treatment — frosted glass wedges (Gaussian blur filter + `--th-frost-overlay` at 0.35 opacity with clipPath), ombré radialGradient slice fills (`color-mix(in oklch, ...)` from surface-blend to full swell color at 0.95 opacity), separator lines on bisector radials (0.5px, 0.32 opacity), tangent-rotated labels (fontSize 13, fontWeight 500, fillOpacity 0.85), updated polygon stroke (1.5px, 0.85 opacity). SIZE bumped to 360, RADIUS to 130, viewBox uses VB_EXTENT with `overflow: visible`. SVG def IDs prefixed `fr-` to avoid collision with the live radar

- Install flow (2026-05-28, ADR 0014): three layers to surface PWA install at the right moments. Layer 1: onboarding final step after Personalize, shown only on mobile non-standalone browsers — iOS gets a three-step share-button tutorial with downward arrow hint, Android triggers native `beforeinstallprompt` when available (falls back to manual steps), desktop and already-standalone skip entirely. "Not now" proceeds to dashboard. Layer 2: recurring dismissible tile at the bottom of the Motions list for non-standalone mobile users — 14-day / 30-day / never cooldown ladder on dismiss, permanently hidden once standalone detected. Layer 3: Settings row renamed from "Add to home screen" to "Install Onduler." All state in localStorage (device-scoped). `lib/install.ts` centralizes platform detection + state + `beforeinstallprompt` lifecycle. `app/components/InstallInstructions.tsx` is the shared tutorial component. `/welcome` stays as the permanent server-rendered reference page for the Settings link. No schema changes
- Engagement layer v1.1 (2026-05-28): three moves from `docs/specs/engagement-layer.md` shipped. **Move A (Shape as dashboard front door):** compact 140px live radar at the top of the dashboard above the checklist, using `lib/radar.ts` geometry (wedges, slices, shoulder polygon, ombré gradients). Shows an empty pulsing circle before any logs; fills in as swells receive credit. Hides during wave mode. Tapping navigates to `/anchors`. No labels, no drag, no chrome — the shape carries the meaning. `app/dashboard/components/DashboardShape.tsx`. **Move B (Visible morph on every log):** `DailyChecklist` dispatches `onduler:log-committed` CustomEvent with motion ID, swell IDs, per-swell weights, and pts/hrs values on every optimistic log. `DashboardShape` subscribes and applies weighted deltas to its actuals array with a 250ms CSS `transition` on slice and polygon `d` attributes, so each log produces a visible shape morph within ~300ms of the tap. **Move G.1 (Pre-unlock wake audit):** confirmed the LockedPage at `/anchors` already renders the live wake from the user's actual logs (not the seeded-random `generateRandomWake` used on marketing surfaces). No code change needed. No schema changes for any of the three moves

- Dashboard polish batch (2026-05-28): filter defaults flipped to show-all (completed, swells, pts/hrs all visible by default); filter labels renamed from "Show X" to "Hide X"; new "Hide wake" toggle in filter popover (localStorage-persisted) conditionally hides the dashboard shape. Anchors page subtitle copy changed from "Your swells this week" to "Your wakes this week" (all cadences). Calendar view: week nav ("Last week / This week"), `‹ Last` / `This ›` buttons, and S M T W T F S day labels pinned into the sticky header; submotion rows given proper `gap-2` spacing and consistent text styling (indent alone distinguishes them). Wake shape repositioned: switched from `sticky` to `fixed` bottom positioning so it stays locked to the viewport; transparent on the motions page, frosted sea foam green (`color-mix` blend of `rgb(144, 216, 196)` and theme `--th-bg`) with `backdrop-blur-md` and full-width in calendar view; `pb-44` bottom padding when wake is visible so content scrolls past the overlay. "Go to motions" button removed from `/welcome` install page
- Waypoint polish batch (2026-05-29): waypoint edit cards overhauled — Save/Cancel buttons replaced with auto-save on blur; click-outside and X button to close; X button uses inline flex layout (not absolute positioning). Recurring edit card gains a Motion dropdown to link/unlink a motion from the swell's motion list (linked waypoints show "Tracks automatically from motion logs" hint and render static non-clickable progress). One-shot edit cards gain bonus points input. Recurring hit behavior changed to additive clicks with proportional ring fill (`stroke-dasharray` based on `currentCycleCount / targetCount`), reset on click past completion via new `resetCycleHits` server action. Target count capped at days-in-cycle (7 for weekly, 31 for monthly) enforced in both UI inputs and server actions (`cadenceMax` helper). Radar scale made fully dynamic — `POINTS_SCALE.floor` dropped from 100 to 1, `hardCeiling` set to `Infinity` (was 1000); `HOURS_SCALE` similarly (floor 0.5, no ceiling). Chart now scales exactly to the data so a 5-point target fills the radar the same way a 3000-point target does

- Tester recruitment postcards ordered (2026-05-30): 100 units printed via Moo (Original 16-17pt paper, matte finish, coated on back only so the cream paragraph side stays pen-friendly for handwritten "Hey {name}" notes; total $61). UTM-split into two batches via Moo Printfinity: half with QR pointing at `?utm_content=surf` for surf-shop drops, half with `?utm_content=climb` for climbing-gym drops. Six weeks post-distribution, query `waitlist` and `auth.users` grouped by `utm_content` to see which audience converts. Design files in `marketing/tester-recruit-2026-05/ready to print/`: three SVGs (surf front, climb front, shared back) with deep ocean ground, layered WaveField, seed=26 n=6 wake polygon rotated 90° (visual centroid shifted to true card center), QR inset at the wake's heart, ONDULER wordmark top-left, "Every motion leaves a wake." tagline top-right in coral. Back has IBM Plex Mono body baked to vector paths via `text-to-paths.py` so Moo's preview (no IBM Plex Mono installed) renders the same as the print. UTM variants regenerated from the front master via `build-utm-variants.py` (uses segno for QR generation, decoder-verifies each variant). Both scripts live in the marketing folder for future variant runs. Awaiting Moo's standard production (5-7 business days) + shipping before distribution to Bay Area surf shops and climbing gyms

- Postcard distribution, first run (2026-06-13): cards went out, but not along the clean surf-batch / climb-batch split the UTM design assumed. **The climb-variant cards got used well beyond climbing gyms.** What actually happened: three climbing gyms (Mission Cliffs, Pacific Pipes, Oaks) allowed only a single card on the bulletin board each; Berkeley Ironworks took a stack. With only climb cards on hand near the beach end of the N Judah while charging the car, one climb card went to a surf shop there (the surf-shop guy liked it), one into the window of a bar (Woods Outbound, agreed), ten to a small arts-and-crafts makerspace (the maker there liked it), one tucked into a random book, and one on a neighborhood community bulletin board (deliberate saturation of that neighborhood, which reads as the right vibe fit). In the Castro: a small pile at the sober center, and one left in a journal at a queer bookstore. Reception ran skeptical-then-warmer once explained in person; one too-cool-for-school passerby said "looks cool" immediately. **UTM consequence for the six-week read:** `utm_content=climb` is no longer "climbing-gym audience" cleanly. It now also covers a surf shop, a bar window, a makerspace, two bookstore/book drops, a community board, and a sober center. Decision (2026-06-13): the surf vs climb split is abandoned. Today's distribution proved the batches won't stay in their intended venues, so `utm_content` is dropped from the analysis entirely (no reprint needed, the tag just goes unused). This supersedes the "group by `utm_content`" plan in the postcards-ordered entry above. The in-person, bulletin-board nature of this run is also why the QR-arrival explainer below matters: most scanners will never read the card back.

- Acquisition source capture decided + in scope (2026-06-13): instead of surf/climb, the signal Josh wants is coarser and reliable, did an account come from a postcard QR or from a direct arrival (typing `onduler.app`, a shared link, search). The QR carries `utm_source=postcard`; direct arrivals carry no `utm_source`. Plan (folded into `docs/specs/signup-explainer-qr-2026-06-13.md`): add `user_settings.signup_source text` (nullable), write the raw `utm_source` at account creation via a hidden form field the signup client reads from `useSearchParams()` (defaults to `direct` when blank), store raw so future channels like a flyer or instagram run are captured without a schema change. Read back with `select signup_source, count(*) from user_settings group by 1`. Hidden-field capture is reliable because the explainer's intro-to-form step stays on the same URL (local step state), so the param survives to submit. Currently nothing captures or persists UTM at all (no analytics tool, no DB column), so this is net-new wiring. Not yet built.

- QR-arrival signup explainer spec queued (2026-06-13): the printed postcard QR points at `/signup?utm_source=postcard&utm_campaign=tester&utm_content=<surf|climb>`, but `/signup` is currently a bare account form with no explanation of what Onduler is. A bulletin-board scanner never reads the card back, so they land cold. Cards are already printed (QR URL is fixed), so the fix lives at `/signup`. Spec at `docs/specs/signup-explainer-qr-2026-06-13.md`: `/signup` becomes a two-step flow for every visitor, an intro screen first (reusing the landing-page breathing wake hero + "Every motion leaves a wake." tagline + the back-of-card explainer copy + a Continue CTA and a Sign in escape hatch), then the existing account form. Intro is informational only, collects nothing, never blocks (skip-is-a-door). UTM params must survive intro-to-form via local step state on the same URL (no path change that would drop the query string). Optional enhancement noted in the spec: point the landing page "Get started" link at `/signup?step=form` so landing visitors (who already read the same pitch on `/`) skip the duplicate intro while QR/direct arrivals still default to it. Not yet built, design-only.

- Signup explainer + source capture shipped (2026-06-13): both of the above are built (`docs/specs/signup-explainer-qr-2026-06-13.md`). `/signup` (`app/signup/page.tsx`) is now a two-step client component with local `'intro' | 'form'` state defaulting to `intro`. The intro reuses the landing hero exactly — same seeded `generateRandomWake(42, 7, 80, {x:100,y:100})` wash + `slow-breathe` outline, `WaveField` backdrop, "Every motion leaves a wake." tagline — followed by the verbatim back-of-card explainer copy, a Get started button (advances via local state, never navigates, so UTM params survive), and an Already-have-an-account Sign in link to `/login`. The form step is the prior signup form unchanged in behavior (heading "Create your account", "Start riding your tides" subhead kept, email/password, check-your-email confirmation state). `?step=form` is wired to land straight on the form (shareable skip link), and the landing-page "Get started" CTA now points at `/signup?step=form` so landing visitors (who already read the same pitch on `/`) skip the redundant intro while QR/direct arrivals still default to it. This is safe for capture: `?step=form` is the same `/signup` route (just a different starting step), so `utm_source` still survives, and landing visitors carry no `utm_source` anyway (they're `direct` regardless). `/welcome`'s Sign up link stays on `/signup` (no pitch there, so its visitors should see the intro). `useSearchParams()` is wrapped in a `<Suspense>` boundary at the page root. **Source capture:** `user_settings.signup_source text` (nullable) added — migration `scripts/migrate-signup-source.sql`, applied to the live DB, folded into canonical `supabase-migration.sql`. The form carries `utm_source` as a hidden input read from `useSearchParams()`; `signUp` (`app/actions/auth.ts`) maps blank → `'direct'`, otherwise stores the raw value, and writes it once via the service-role `createAdminClient()` keyed by the new user id with `ignoreDuplicates` (ON CONFLICT DO NOTHING — write-once, no overwrite on re-signup). The admin client is required because email confirmation means there's no session/RLS context at signup; the write is best-effort (try/catch) so attribution can never block a signup. Read back: `select signup_source, count(*) from user_settings group by 1`.

- Behavior change layer (2026-06-01): Moves 1, 2, 3, 5 from `docs/specs/behavior-change-layer.md` shipped. Move 4 deferred to native phase. **Move 1 (Intensity picker):** long-press on motion checkbox opens a light/medium/deep popover; each chip shows the literal pts (or hrs) it will commit. Multipliers (0.5×/1×/1.5×) applied at log time to points and hours. Single-tap logging unchanged (medium). Schema: `logs.intensity text NULL DEFAULT 'medium'`. Shared constants in `lib/intensity.ts` (extracted from both client and server to avoid `'use server'` export issue). **Move 2 (Intentions in ceremony):** observation + intention merged into one ceremony screen; prior week's `intention_text` surfaces as placeholder text on the next week's expectation prompt, closing the reflection loop. Schema: `reflections.intention_text text NULL`. **Move 3 (Friction audit):** bucket seed chips on empty state ("Morning rituals" / "Wind down"), contextual copy on bucket creation ("Cluster motions by when they happen"), confirmed first motion row sits above the fold on iPhone 14/15 (390×844). **Move 5 (Bucket copy edit):** empty-state copy updated to "Cluster motions by when they happen."
- Personalization features (2026-06-01): background photo upload with drag-to-reposition and pinch-to-zoom overlay, frosted-glass sticky headers, client-side image compression (max 1920px, JPEG 85%). Custom progress bar color picker on dashboard, swells aggregate bar, and anchors daily bars. Position string format `"x y scale"` persists zoom level across sessions. BackgroundApplier uses fixed-position DOM element (not `background-attachment: fixed`) for iOS Safari compatibility
- Bug fix (2026-06-01): shared intensity constants moved to `lib/intensity.ts` to avoid `'use server'` directive export issue (server actions cannot re-export constants consumed by client components)
- Anchors refinement v1 (2026-06-05): four moves from `docs/specs/anchors-refinement-2026-06-05.md`. (1) Calendar icon on the Anchors sticky bar swaps to a hand-drawn spiral-notebook glyph and navigates to `/anchors/journal` (Motions and Swells calendar icons unchanged — still the "go correct your record" affordance there). (2) Standalone "See all in your journal" link below the inline anchor log is gone — calendar is the single journal affordance now. (3) `InlineAnchorLog` renders unconditionally right under `SwellRadar`, above the `totalValue === 0` gate, so anchors show in logless periods; new section order on the unlocked Anchors page is Radar → Anchors → (gate)[By swell → Daily → Waves] → Coming together. (4) Anchor entry titles ("Week ceremony", "Month ceremony", "Anchor") use the same `text-sm font-medium text-th-text` treatment as motion-row titles — the journal voice now reads as the star of the page rather than a kicker
- Sticky frosted pill (2026-06-05): rewrote `body.has-bg-image .sticky.bg-th-bg` to stop using a `::before` pseudo extending `left:-100vw; right:-100vw` past its parent (the original sticky frost technique). The frost (backdrop-blur + 80% bg-tint) now applies directly to the sticky element itself, contained inside the column max-width with `border-radius: 1rem` and 1rem horizontal padding so content breathes inside the rounded edges. **Side effect: fixes the horizontal-overflow / zoom-out bug** — the old -100vw extension made the page's `scrollWidth` ~200vw + parent, which `overflow-x-hidden` clipped visually but pinch-zoom-out on iOS Safari (and Cmd+− on desktop) still revealed as a white area to the right. With the pill contained, there's nothing to overflow, no clip workaround needed, and the photo-render shift artifacts that any `overflow-x: clip` workaround produced are also gone. The intentional pill shape ended up being more on-brand for the aesthetic brief than the full-bleed frost it replaced
- Floating pill nav top + bottom (2026-06-05, spec `docs/specs/bottom-nav-floating-pill-2026-06-05.md`): bottom nav becomes a free-floating detached pill (rounded-full, `bg-th-surface`, `shadow-lg`, inset from edges, `pointer-events-none` positioning layer with `pointer-events-auto` on the pill itself so taps in the gaps pass through to content). Sticky tops across Motions/Anchors/Swells/Settings/Proficiency pick up matching pill treatment via globals.css scoped to `.sticky.top-0.bg-th-bg` (mobile only via `@media (max-width: 767px)`, so SideNav at md+ is unaffected); inline `paddingTop: calc(safe-area + 0.5rem)` dropped at each sticky site since CSS now owns padding. Both pills sit at `safe-area + 1rem` from their respective edges. Body bottom-padding `4.5rem → 5rem` to clear the floating nav; DashboardShape offset `2.75rem → 4.75rem` and undo toast `bottom-6 → 5.5rem + safe-area` to clear the floating pill too. Bottom pill picks up the same backdrop-blur frost as the top when a background photo is set (new `.floating-pill-nav` marker class + `body.has-bg-image .floating-pill-nav` CSS rule). Onboarding sticky opts out via `style={{ top: 0 }}` instead of `top-0` class so its `pt-12` brand-spacing pattern stays. New `PinTopApplier` client component + Settings → Appearance "Floating top bar" toggle (Pin / Unpin) flips `body.unpinned-top`, persisted in localStorage (`onduler-pin-top`); CSS rule `body.unpinned-top .sticky.top-0.bg-th-bg { position: static !important; box-shadow: none }` lets the top scroll away with the page when unpinned. Settings page: Send feedback + Sign out moved from a fixed bottom footer into the top floating pill, below a divider, justify-between (Sign out left, Send feedback right) at `text-base font-medium`. Week label drops weekday across Motions/Swells/Anchors ("Week of Sunday, May 31" → "Week of May 31") since the longer form wrapped to a second line
- Brand color system + visual polish pass (2026-06-10): introduced a single `--brand` CSS token (per-theme defaults — Tjornuvik #0BC85B light / #72F189 dark, Biarritz #14225D light / #F67E4E dark, Bolinas #87AE73 light / #F2C421 dark) and a `.brand-text` utility that paints the wordmark, the floating-pill `+` glyph, and the pill "Onduler" label with a `--brand-gradient` (solid → surface-mixed sweep) via `background-clip: text`. The 11 "Onduler" labels and 6 `+` buttons across the app picked up `brand-text`; `loading.tsx` wordmark too. New `app/components/BrandColorApplier.tsx` lets the user override `--brand` from Settings (the "Progress bar color" row renamed **Accent color** — same color picker, same Reset button, but the swatch now reads live from `var(--brand)` after Reset). Six default progress-bar fallbacks across the app (`var(--th-accent)` → `var(--brand)`) so a single knob drives wordmark/pill/+/progress bars/active-nav. BottomNav + SideNav active state split: icon gets `color: var(--brand)` (solid for SVG `currentColor`), label gets `brand-text` (gradient sweep). **Anchors locked page** rebuilt: outer container becomes `fixed inset-0 z-0` filling the whole viewport with `bg-th-bg`, top floating pill (Onduler + plus) restored inside, wave area `flex-1` extends to bottom (BottomNav floats over via its own `z-40`). **Wake breathing animation** synced — both layers (blurred halo + outline stroke) now share the `slow-breathe` keyframe instead of only the outline moving against a static halo; outline thickness 0.6→0.9, opacity 0.55→0.7. **Coming Together** card on Anchors now forces always-dark by inlining Tjornuvik-dark CSS vars on its wrapper so its WaveField + LockedCadenceTile children cascade dark regardless of theme/OS. **Journal page** wraps heading + plus + description into a single sticky floating pill (using the standard `.sticky.top-0.bg-th-bg` pill machinery). **Motion creation** gets a "Feeds" multi-select swell chip row in `AddMotionForm` matching the detail-sheet aesthetic; selections serialize to a hidden `swell_entries` JSON field; `createMotion` bulk-inserts the resulting `motion_swells` rows. The single-`swell_id` path from `/swells/[id]` is preserved. **Photo-bg readability rewrite:** `body.has-bg-image` now forces Tjornuvik-dark text tokens (`--th-text/secondary/muted/faint`) with `!important` regardless of OS mode, then restores them per-theme inside `.sticky.bg-th-bg` and `.floating-pill-nav` (Tjornuvik / Biarritz / Bolinas × light/dark) with matching `!important` so pill specificity wins. Body shadow locked to `rgba(0,0,0,0.7)` always (the OS-mode white/dark mix is gone — body text reads better with one dark halo against light Tjornuvik-dark copy on any photo). Pills opt out of the shadow. **Misc fixes:** removed the 32px `--th-bg → transparent` gradient bands on the top + bottom of uploaded photos in `BackgroundApplier` (the "black hazy border" complaint); added `.pill-day-col` class + matching `margin-right: 1rem` rule (mobile or has-bg-image) on the day-box column in `WeekEditView` so the day-box right edges align with the day-label right edges inside the floating pill; removed the `router.push('/anchors')` click handler from `DashboardShape` (was creating a navigation loop); progress-bar tracks outside the floating pill switched from `bg-th-surface` to `bg-th-surface/40` so a photo can show through unfilled portions; `DashboardShape` empty-state circle filled with a top-to-bottom ombre derived from `--brand` (with progressBarColor override) instead of just a thin outline ring
- Onboarding + tutorial + import fixes (2026-06-12, testing batch): **(1) Wake always visible** — removed the two `>= 3` swell-count gates (`DashboardView` render site + `DashboardShape` early return); the breathing circle now renders from first landing regardless of swell count or motion_swells assignments, polygon still requires 3+ swells (a polygon needs 3 vertices — under 3 the circle stays). Wave-mode hide and "Hide wake" filter unchanged. **(2) Motions hint rewritten** to the real interaction model (checkbox logs, row opens detail sheet, grip drags — old copy claimed a kebab that doesn't exist) and made tracking-mode-aware via the already-present `trackingMode` prop (hours variant uses HH:MM examples). Other four hints audited clean — currency-neutral, no drifted claims. **(3) Onboarding swells step** — "+ Add your own" pinned into the sticky header (new `headerExtra` slot on `ScreenShell`), archetype pack grid moved below the list, "All swells" → "Example swells", **minimum raised from 1 to 3 swells** (CTA counts down "Add 3 more" → "Add 1 more" → "Next →"; step description says "Add at least 3") so every shape surface works from day one. **(4) Tracking-mode copy** — "Points give extra weight to motions that matter most. Hours track actual time spent." **(5) Onboarding background photo** — optional upload on the personalize step directly after the theme picker, reusing the Settings pipeline (`resizeImage` extracted to `lib/image.ts`, `ImageAdjustOverlay`, `uploadBackground`/`setBackgroundPosition`/`removeBackground`); thumbnail + Adjust/Remove once uploaded; skipping renders nothing. **(6) Import clear options** — preview step gains "Your existing swells and motions" (shown only when existing data exists, via head-count queries): Keep (default) / Archive (new `archiveChapterForImport()` — chapter rollover without resetting `onboarding_complete`, shared `rolloverChapter()` core extracted) / Delete (hard-deletes active-chapter motions → swells → buckets before import; logs cascade; two-tap confirm + red warning). `confirmImport(preview, clearMode)` signature; auto-enables `groups_enabled` when the parse includes buckets; import-flow copy says "bucket(s)" per ADR 0012. **(7) Journal deletes** — `deleteFreeAnchor` generalized to `deleteAnchor` (ceremony anchors now deletable too — the journal is the user's library, decision 2026-06-12, supersedes the frozen-record rule from ADR 0007 for deletion only); per-anchor two-tap Delete on journal cards; archived chapters get a two-tap "Delete chapter" control under their header (`deleteArchivedChapter(id)` refuses the active chapter; all chapter_id FKs are ON DELETE CASCADE so the row delete removes everything). **(8) AI option in onboarding** — "Or ask your AI" section on the swells step below the archetype packs: copy the shared `IMPORT_PROMPT_TEMPLATE`, paste the reply, and `parseImportMarkdown` runs client-side feeding the normal onboarding state (imported swells land picked — name-matched seeded swells just get picked, the rest become custom entries with palette colors; motions land pre-assigned to their first swell and skip if unassigned; groups/targets dropped — onboarding has no surface for them). `completeOnboarding` motions param gains optional `points`/`hours` so AI-assigned values survive (manual adds still default 1/1.0). Prompt template rules updated to current spec: 3-6 swells (at least 3, "to draw its shapes"), every swell must have at least one motion under it and vice versa — the Settings import page shares the same template so it inherits the rules. **(9) Motion-derived swell targets** — replaces the 100 pts / 5 hrs constants: `completeOnboarding` and `confirmImport` derive each swell's weekly target from its feeding motions (4 logs/wk each in points, `ceil(4 × Σ pts×weight)`; 3 logs/wk in hours, 0.25-hr ceil); the AI prompt now requires `[target: N]` with the ×4 rule stated and the example doc modeling it; import always writes both target columns (was leaving the off-mode column null); bare-creation sites (`createSwell`, `adoptBuild`) drop to a 4 pts / 3 hrs floor (corrected 2026-06-12 from 12 pts, which let a motionless swell outrank a 1-motion swell — see Locked defaults note). See the amended Locked defaults note under ADR 0002. **(10) Silent-failure sweep** — every server action now checks its Supabase writes and returns `{ error }` (≈55 writes guarded across settings/motions/logs/swells/groups/milestones/chapters/theme/wave/welcomeback/builds/billing; batch reorders collect Promise.all results; two deliberate non-failing checks: the waypoint-hit post-hook in quickLogMotion and confirmImport's groups_enabled upsert log via console.error since the primary write already succeeded). New `app/components/useSaveGuard.ts` hook: `await guard(action(...))` takes the PROMISE (not the awaited result) so its try/catch catches thrown transport failures (offline "Failed to fetch") as well as returned `{ error }`s; on failure shows a toast ("That didn't save. Check your connection and try again.") and resyncs via `router.refresh()` — but while `!navigator.onLine` the refresh is DEFERRED to a once-only `online` listener (refreshing with no network swaps the page for Next's "couldn't load" shell, worse than briefly-stale optimistic state; verified live 2026-06-12). Resolves boolean so follow-up navigation gates on success. Wired into ~70 call sites across 17 client components (DailyChecklist, SortableMotionList, WeekEditView, MotionDetailSheet, SwellsView, SwellsList, SwellProficiencyView, MotionSwellToggle, MilestonesSection, SettingsPanel, EditGroupForm, ShapePicker, AnchorsToolbar, WaveGrid, CycleCeremony). Sites with pre-existing inline error UI (CadenceSection, InlineAnchorLog, ImportFlow, JournalClient, AddMilestoneForm) left on their own handling. HintCard dismissal stays intentionally silent. **(11) Onboarding draft persistence** — durable onboarding inputs (step, swell picks incl. seeded colors, motions, theme/tracking/haptics/celebrations) mirror to localStorage (`onduler-onboarding-draft`, versioned) on every change and restore on mount instead of reseeding, so a mid-flow refresh no longer wipes the user's setup. Transient edit state (open forms, half-typed names, AI paste box, bg photo — already server-persisted) is not drafted. Cleared up-front on completion (the server action redirects, so post-await code can't be trusted to run) and written back if completion errors; transport failures during completion surface as a friendly error instead of an unhandled throw. Device-scoped like the install-flow state, so a shared device could surface one user's abandoned draft to another — accepted for v1

- Weekly-target celebration (2026-06-12): the dashboard celebration was sidetracked — the swell-colored tideline (`CelebrationOverlay` + `celebration-tideline` keyframe) actually fired on *every* motion log, and `swellTargets` was passed into `DailyChecklist` but unused (`_swellTargets`), so nothing detected a target cross. Resolved as TWO celebrations, both gated by `celebrationEnabled`: (a) the per-motion tideline stays exactly as-is (fires every log), and (b) a NEW full-screen wave sweep fires *additionally* when a log pushes a swell over its weekly target. New `app/dashboard/components/WaveSweepOverlay.tsx`: transparent fixed canvas (clears each frame, never fills a bg, so it passes OVER the live screen — unlike `WaveField` which is opaque), two bands of stacked translucent sine lines (~0.16–0.28 opacity) sweeping in from left and right and crossing in the middle, each an ombre wake (full at the crest, fading to transparent behind) tinted with the *triggering swell's* color, ~1.25s then fade + `onDone` unmount; `prefers-reduced-motion` gets a brief static wash. Trigger lives in `DailyChecklist.handleLog`: reads `swellProgressRef` before it's mutated, and when `before < target && after >= target` for any fed swell, sets `targetSweep={color}`. `CelebrationOverlay` and `WaveSweepOverlay` render under independent guards (a fragment) so the sweep isn't unmounted early when the tideline finishes. Fires from BOTH logging surfaces: the daily checkbox (`handleLog`) and the weekly calendar / Views mode (`WeekEditView.handleTapDay`, gated to the current week, using the passed `swellWeeklyProgress` which is fresh because the calendar refreshes per log). `swellProgressRef` now re-seeds from `swellWeeklyProgress` via a sync effect so the daily path's baseline isn't left stale after a calendar refresh (without it, a cross right after calendar logging compared against a mount-time baseline and silently missed). **Monthly-target celebration deferred** — needs month-to-date per-swell aggregation + monthly targets plumbed from `dashboard/page.tsx` (not currently fetched); the overlay + trigger structure already supports it, only the data is missing. Settings "Visual" toggle copy is "A ripple each time you log, a wave when you cross a weekly swell target" — covers both celebrations under the one switch. **Visual design (iterated live 2026-06-12):** the sweep became a field of ~28 stacked sine ripples that rise up from the bottom and dissolve toward the top, with WaveField-style depth via "fill-below" erase (each ripple clears what's behind it below its line, so they layer like water instead of crossing like ribbons — done with destination-out, not an opaque fill, so it stays transparent over the live screen), a low translucent water body (TINT) under the surface bounded by a final erase below the front ripple, amplitude growing toward the front so a jagged ripple carries the bottom edge, a swell→brand horizontal ombre on every line and the body, and a smoothstep opacity fade by absolute screen height (clear top third, full toward the bottom). Constants at the top of `WaveSweepOverlay.tsx` (DURATION/RISE/TINT/N/topFade) are the tuning surface. The temporary `?wavetest` preview button was removed after the look was locked. **Light-theme contrast (2026-06-13):** the overlay reads `--th-bg` luminance and applies a 1.5× opacity boost on light backgrounds (dark, where it's tuned, stays at 1) so the celebration stays legible without losing its calm in light mode

- Admin engagement metrics (2026-06-13): `/admin` gained an Engagement section (Activated = distinct users who ever logged, Logged 7d = distinct users with a log in last 7d, Logs 7d = log volume) computed by fetching `logs(user_id, logged_at)` and reducing in JS (no chapter scoping — any activity counts), plus a last-logged column on Recent signups. Motivation: `auth.last_sign_in_at`-based Active 7d/30d under-reports for a persistent-session PWA; logs are the honest activation/retention signal. First read surfaced the activation pattern below

- Onboarding first-log practice (2026-06-13): new `firstlog` step between personalize and install/finish, shown only when the user has motions feeding picked swells (skipped if they "Skip and use defaults", per the skip-is-a-door doctrine). Renders their motions as a mini checklist; tapping a checkbox toggles a done visual (check + line-through) and surfaces "That's it. Every motion you log shapes your wake." Purpose: teach the core log gesture inside the converting first session instead of relying on a day-2 return that early signups weren't making. **Critical constraint (Josh): these demo taps are purely client-side and NEVER written to the DB** — `completeOnboarding` still writes zero logs, so the real dashboard starts genuinely empty. No fake data in the user's wake (fake activity would corrupt the "how you really show up" signal from day one). Copy reassures: "This is just practice, nothing is saved. Your tracking starts fresh on the next screen." Verified live: demo tap → dashboard loads at 0 pts, no persisted log. (Triggering admin read was caveated — most of those early signups predated current onboarding or were friends/family.)

- Pre-launch DB hardening (2026-06-13, from Supabase advisors): RLS was already enabled on all 13 public tables with proper `own X` owner policies — no integrity holes. Applied to prod via MCP migrations + reflected in `supabase-migration.sql` and `scripts/migrate-prelaunch-db-hardening.sql`: (1) wrapped `auth.uid()` in `(select auth.uid())` across all 11 owner policies via `ALTER POLICY` (evaluate once per query, not per row — cleared the `auth_rls_initplan` advisor); (2) added standalone `chapter_id` indexes on groups/logs/motions/reflections/swells/wave_checkins (covers the FK cascade on chapter archive/delete — cleared `unindexed_foreign_keys`); (3) replaced the public `backgrounds` bucket's broad SELECT policy (which let any client *enumerate* every user's uploads) with a per-user-path-scoped SELECT — bucket is public so `<img src>` URLs are unaffected, and `removeBackground` (lists own folder) still works; cleared `public_bucket_allows_listing`. Manual drift check passed: prod has the `logs_intensity` / `milestones_kind` / `motions_submotion_mode` CHECKs and all `chapter_id` + `user_id` FK `ON DELETE CASCADE`s. **Still TODO (dashboard, no SQL): enable leaked-password protection** (Auth → Password settings) — matters more because email confirmation is off this wave, so the password is the only credential gate. Left intentionally untouched: the `contact_submissions` / `waitlist` public-form `always_true` INSERT policies, and the 5 pre-existing "unused index" warnings (they match real query paths, just no traffic yet). **Update 2026-06-13: leaked-password protection is a Supabase Pro-tier feature, so it stays OFF on the free plan.** Free-tier substitute (worth setting): minimum password length + required character classes in the auth password settings.

- Auth email config (2026-06-13, Supabase dashboard — not in repo): two separate email systems. (1) **App emails** (weekly wake email, cron `0 15 * * 0`) send via Resend from `Onduler <hello@onduler.app>` (`lib/email.ts`); the `onduler.app` domain is verified in Resend (DKIM + DMARC `p=none` published). (2) **Auth emails** (password reset; signup confirm when later enabled) go through Supabase Auth, which by default uses `noreply@mail.app.supabase.io` — rate-limited (~2/hr) and spam-prone, and shows no Onduler branding so users can't search for it. Fixed by pointing Supabase Auth at **custom SMTP → Resend** (Authentication → Emails → SMTP Settings: host `smtp.resend.com`, port 465, user `resend`, password = `RESEND_API_KEY`, sender `hello@onduler.app`, name `Onduler`; raises the auth-email rate limit to 30/hr) and **branding the Reset Password template** (Authentication → Emails → Templates: subject "Reset your Onduler password" + Onduler-voiced body with the "Every motion leaves a wake." sign-off). Verified live: branded reset email arrives from `hello@onduler.app`, in inbox (a spam flag on the very first sends is just new-domain reputation, warms with volume). When email confirmation is turned on for the larger wave, brand the Confirm Signup template the same way.

- Encryption slot management — step 7, part 1 (2026-06-19): two gaps in the content-encryption key lifecycle closed. (1) **`changePassword` password-slot re-wrap** — a password content-slot is wrapped under the *account* password, so changing the password previously stranded it (decryptable only with the old password). `ChangePasswordForm` in `app/settings/SettingsPanel.tsx` now wraps the server action: change the account password, then re-wrap the password slot under the new one client-side (`addPasswordSlot(dek, newPw)` → `persistPasswordSlot`), since the DEK lives only in the browser. Passkey-only users (no password slot) skip it — no new surface. Guards: refuses before changing anything if a password slot exists but the DEK isn't loaded ("unlock first"); surfaces a clear recovery message if the re-wrap fails post-change. (2) **Settings → Privacy slot-management UI** — new collapsible section under Account backed by `app/settings/KeyManagement.tsx`: lists enrolled passkeys (add via the Supabase WebAuthn+PRF `registerPasskey` ceremony, two-tap remove), toggles the password fallback on/off (enable asks for the account password), and shows recovery-code status. Guard rail throughout: never drop below one primary unlock (can't remove the last passkey with no password, or the password with no passkey); adding a slot needs the DEK in memory, removing one doesn't; all removals two-tap. Reuses existing actions (`getEncSetupState`/`getKeyEnvelope`/`persistPasskeySlot`/`persistPasswordSlot`/`removePasskeySlot`/`removePasswordSlot`) and crypto primitives — no new actions, no schema change. Typecheck + lint + `npm run build` clean. **Not done (deferred to step 7 part 2):** locked-out reset flow — re-wrapping the password slot after a `/reset-password` forgot-password unlock (no DEK in memory there, so it still strands the slot — same known gap as before for that path only), recovery-code regeneration, and a recovery-only lockout path. WebAuthn add-passkey ceremony not yet exercised on a real device against `onduler.app`'s RP ID (wiring mirrors the proven `KeySetup` path).

- Onboarding carousel entry screen (2026-06-19): the logged-out landing (`app/page.tsx` → `LandingPage` → new `app/components/onboarding/OnboardingCarousel.tsx` + `OnboardingVisuals.tsx`) is now a 5-slide swipeable onboarding carousel with persistent sign-in actions, replacing the old single-scroll marketing landing. CSS scroll-snap track (no carousel dep), pagination dots synced to scroll + tap-to-go, full-bleed `fixed inset-0` over the existing `WaveField` swell (safe since logged-out has no nav). Slide order: **Motions** (mock checklist, "Cook" checks itself off on entry) → **Swells** (mock contribution bars, Health fills on entry) → **Anchors** (mock journal, last sentence types in — "Walked by the water after work. / Felt clearer afterward than I expected.") → **Privacy** (dimmed wake behind an Onduler-styled lock with a soft edgeless halo) → **Wake finale** ("Record when you can. / Pause when you want.", breathing wake filled with a tri-theme-brand ombré: Tjørnuvík green → Bolinas gold → Biarritz coral). Mocks reuse the real app's row/bar/entry class patterns with static data. Per-slide animations (re)play on becoming active via keyed remount; layout is headline-above / body-below the visual. Persistent brand header: "Onduler" in `.brand-text` ombré on every slide; "Every motion leaves a wake." (solid `--brand`) held back until the finale, then fades in (baseline-aligned). Bottom actions: **Get started** → `/signup?step=form` with the `--brand-gradient` ombré (primary); **Sign in** → `/login` with a `linear-gradient(90deg, --th-muted, #f5c87e)` ombré; fine print (Terms/Privacy + install link) in a shared cream. Routing preserved exactly from the old landing (no auth regression; `/signup` URL invariant held). Copy is affirmative-only per the onboarding copy rules. Approved live by Josh. **Light-mode legibility pass (2026-06-19):** the original bottom buttons set their label to `text-th-bg`, which is near-white in light mode, so the green `--brand-gradient` (Get started) and the fixed `#f5c87e` gold gradient (Sign in) both dropped to ~1.3–1.8:1 there (they only ever read because dark mode's `text-th-bg` is near-black). Fixed by introducing two reusable tokens in `globals.css`: a per-theme **`--brand-fg`** (the legible label color for that theme's brand fill — dark ink for Tjørnuvík/Bolinas, *light* ink for Biarritz whose brand is dark navy `#14225d`, the case a single fixed color would have broken), and **`--brand-fill`** (a CTA gradient that stays in-hue, `brand → 78%-darker brand`, instead of `--brand-gradient`'s fade-toward-surface — so one `--brand-fg` label reads across the whole gradient in every theme/mode). Get started now uses `--brand-fill` + `--brand-fg`; Sign in keeps the warm `--th-muted → #f5c87e` gradient but switches to a dark warm label (`#241a10`) legible on the light gold in both modes. Per Josh's call, the colorful-gradient look was kept (not flattened to solid/ghost). The old signup `IntroScreen` (reached only via direct `/signup`, e.g. a postcard QR) had its pre-redesign negative-framing copy fixed in the same pass — dropped "No streaks, no guilt, nothing to fall behind on." and the "tasks" line for affirmative, carousel-consistent copy. Merged to `main` + deployed 2026-06-19.

- Polish batch (2026-06-20): four small fixes. (1) **Import page relabeled for discoverability** — the JSON restore (`RestoreFlow` at `/settings/import`) was live but hidden under a section labeled only "Import from your AI"; Settings → Data row is now "Import or restore data" / "Restore a backup, or set up with an LLM", and the page `h1` is "Import or restore" with the AI flow demoted to its own "Set up with your AI" `h2` (sibling of the existing "Restore from a backup"). No logic changed. (2) **Swell-page add-motion now shows the swell multi-select** — opening the add-motion form from `/swells/[id]` previously auto-linked to that one swell with no picker; it now renders the same "Feeds" chip multi-select as the dashboard, with the contextual swell pre-selected (one-tap keep, or deselect / add others). `AddMotionForm` shows the picker whenever `allSwells` is passed (no longer gated on `!swellId`); the proficiency view passes its `allSwells` prop through; the hidden single-`swell_id` input is now only a fallback for when the picker isn't shown. Both link paths insert `contribution_weight: 1`, so no weight change. (3) **Dashboard undo toast no longer covers the wake** — the `undoToastEl` in `DailyChecklist` moved from `bottom: 5.5rem` (which sat on the bottom-pinned wake) to just under the sticky header. Because the header height varies across flat / by-swell / Views modes, it's positioned by measuring the live header's bottom edge: a `ResizeObserver` callback ref (`headerRef`) attached to all four mode headers stores `headerBottom`, and the fixed toast sits 8px below it (fallback `safe-area-top + 6rem` pre-measure). The separate global `Toast.tsx` (cross-swell-drag toasts only) was also moved to `top: safe-area + 1rem` for consistency. (4) **Anchors locked page stopped covering the desktop SideNav** — `LockedPage`'s `fixed inset-0` spanned the whole viewport from x=0, painting over the `md+` `w-60` SideNav rail; added `md:left-60` so the full-bleed locked experience starts after the 15rem rail on desktop (mobile unchanged — no SideNav there). All four committed to `main`; **not yet pushed/deployed** as of session end.

- Tablets get the phone app shell (2026-06-20): the `md+` desktop sidebar shell (SideNav vs BottomNav, body `flex-row`, content offset — shipped 2026-05-21) was tripping on iPads, which are all ≥768px wide, so a tester's iPad showed the "web version" instead of the phone-style app. Fixed by gating the shell switch on a **fine pointer**, not just width: new Tailwind v4 `@custom-variant desktop (@media (min-width: 768px) and (pointer: fine))` in `globals.css`, and the four shell switches swapped `md:` → `desktop:` (`app/layout.tsx` body, `app/AppShell.tsx` content column incl. `desktop:lg:max-w-4xl`, `app/components/SideNav.tsx` `desktop:flex`, `app/components/BottomNav.tsx` `desktop:hidden`). The two mobile-only floating-pill media queries in `globals.css` widened from `(max-width: 767px)` to `(max-width: 767px), (pointer: coarse)` to match. Net effect: any touch device (every iPad, any size/orientation — coarse primary pointer) falls through to the bottom-nav app layout; only mouse-driven wide screens (real laptops/desktops) keep the sidebar. iPadOS reports coarse even with a trackpad/Magic Keyboard, so paired-keyboard iPads stay on the app layout too (desired). `npm run build` clean. **Committed + pushed to `main` + auto-deploying to `onduler.app`.** Verify on the actual iPad once Vercel finishes.

- Locked-state graceful display (content-encryption, 2026-06-21): when a user's content is encrypted (`enc_enabled`) but no DEK is loaded on this device — fresh device after a password reset, lost passkey, etc. — the app no longer shows blank labels behind a full-screen wall. Instead it **renders all the plaintext data it has** (points, hours, colors, dates, radar, wake — none of which is encrypted) and shows **`🔒 Locked`** in place of each unreadable name/text. The old full-screen `UnlockGate` overlay became a **non-blocking floating "🔒 Unlock your labels" pill** (above the nav on mobile, bottom-right at desktop) that opens the existing unlock panel (passkey / recovery / password + start-fresh escape); on success labels decrypt live in place, no navigation. **Read-path** (`app/components/useDecrypted.ts`): `useDecryptedReady` now returns a third `locked` state — confirmed-no-key — substituting the `LOCKED_LABEL` sentinel for ciphertext and reporting `ready: true` so views actually paint (was `ready: false` → `return null`); gated on DekProvider `loading` so a user *with* a cached key never flashes a lock; new `useLocked()` helper. `useDecrypted`/`DecryptedText` display sites (radar, journal, etc.) inherit the sentinel for free. **Corruption/leak backstop** (`app/components/useContentCrypto.ts`): `encryptContent`/`encryptFormData` now **throw while locked** (`encEnabled && !dek`), so no save can persist `🔒 Locked` over a real name or leak a plaintext name to the operator. **Read-only sweep** (so the backstop is never hit in normal use): every reachable write surface disables itself while locked — motion name/pts/hrs/submotion-add/submotion-edit/duplicate (`MotionDetailSheet`), swell name/color/weekly-target + both add-motion `+` (`SwellProficiencyView`), waypoint rename/add/cadence (`MilestonesSection`, `CadenceSection`), bucket name+save (`EditGroupForm`), anchor new/inline-edit/edit-form/ceremony (`NewAnchorForm`, `InlineAnchorLog`, `EditAnchorForm`, `CycleCeremony`), settings quick-group + shape presets + import/restore (`SettingsPanel`, `ShapePicker`, `ImportFlow`, `RestoreFlow`), dashboard/swells `+` (`DashboardView`, `SwellsView`), and the by-swell drag "Keep both" duplicate (`DailyChecklist`). **Key constraint found:** editing a motion's pts/hrs or a swell's color/target *re-encrypts the name on save* (the update actions take name as a required arg), so those can't stay editable while the name is locked without new partial-update actions — hence locked = read-only display for v1. Anything that touches no encrypted field stays live while locked: logging/unlogging, delete, hide, reorder, swell/group assignment. **Relabel-in-place is explicitly deferred** to its own phase (needs gating + a visual distinct from the locked state before we ever tell a user their key is gone). **`DekProvider` self-heal (bug found while testing this):** `encEnabled` was loaded only once in the mount effect, off `getSession()`/`getEncEnabled()` — but right after a reload the session can be mid-refresh, so `getEncEnabled()` (which calls `getUser()`) transiently returns false and **nothing re-fetched it**, leaving `encEnabled` stuck false for the whole session. Consequence is twofold and serious: (a) the unlock pill + read-only guards (both gated on `encEnabled`) stay inert even on an encrypted account, and (b) the write backstop in `useContentCrypto` (same flag) is inert too, so **every create/edit during that window silently writes PLAINTEXT** into an otherwise-encrypted account (reads still work — they key off the `enc:` prefix — so it's invisible). Fix: `onAuthStateChange` now backfills `encEnabled` (and the cached DEK if any) on INITIAL_SESSION / SIGNED_IN / TOKEN_REFRESHED. **Data fallout observed on `jryanjacobs` testbed:** editing a swell's color while in this stuck-false state re-sent the name field (then the `🔒 Locked` sentinel) and overwrote that swell's encrypted name with plaintext `🔒 Locked` — unrecoverable from the column (no prior-value history). **Resolved 2026-06-21:** the swell was renamed by hand once unlocked; a pre-existing `test` swell was also plaintext from the same root cause. The only other account (one tester — Josh's mom) hadn't signed in since the fix and was confirmed unaffected, so no outstanding plaintext exposure. **Future-launch note: the race could leave plaintext rows on any account that reloaded during a token refresh before this fix deployed — sweep `name not like 'enc:%'` per encrypted account if the tester pool grows.** Also fixed a React style warning on the proficiency progress bar (`background` shorthand + `backgroundSize` → `backgroundImage`). `tsc` + `eslint` clean on all touched files. **Not yet committed/pushed as of session end.**

**Deferred / not yet built:**
- **Email confirmation stays OFF for the first launch wave (decided 2026-06-13).** Intentional: requiring email verification adds a leave-the-app-and-come-back hop that costs more than it's worth at this scale, where Josh is reaching out to every tester individually anyway. Turn it on in the second, larger pass. When it's enabled, two code changes ride along: (a) `signUp()` must pass `emailRedirectTo: ${NEXT_PUBLIC_APP_URL}/auth/callback?next=/onboarding` so the confirmation link reliably hits the working `/auth/callback` route instead of depending on the Supabase Site URL config; (b) drop the "then sign in" step from the "Check your email" screen copy in `app/signup/page.tsx`, since the callback establishes a session and lands the user authenticated. Until then, signUp returns a session directly and redirects to `/onboarding`.
- Premium feature gating (checking `subscription_status` before allowing AI features)
- Remove admin gate on Crew row when ready to go live
- **Engagement layer v1.2 (deferred):** Remaining moves from `docs/specs/engagement-layer.md`. v1.1 (A + B + G.1) shipped 2026-05-28. Still ahead: D (week as anticipation copy), E (additive continuity line). v2 candidates: C (morning prompt), F (notification voice rule), G.2 (post-unlock recent anchor peek).
- **Behavior change layer Move 4 (share sheet / social proof) deferred to native phase.** iOS PWA cannot register as a Share Sheet target; push notification infrastructure also requires a native shell. See `docs/specs/behavior-change-layer.md` §4.

## V1 exit criteria

Onduler v1 is **done** — ready for wider release — when both of the following hold simultaneously:

1. **At least 5 testers have completed a full weekly cycle-close ceremony** (or intentionally skipped one — both count). The weekly ceremony is the strategic-reflection surface that distinguishes Onduler from streak-and-checklist habit apps; if testers don't reach it at least once, the product hasn't been tested at the layer that matters most.
2. **Two consecutive weeks of tester feedback surface no surprises.** A "surprise" is feedback that contradicts the current spec — a design assumption wrong, the voice landing differently than intended, copy reading badly, a behavioral pattern not predicted. Two weeks of expected-only feedback means the product has been *understood* and the remaining work is polish.

Both conditions must hold. Either alone is insufficient: condition 1 without condition 2 means people are using it but the design's still being figured out; condition 2 without condition 1 means we think we understand it but nobody has tested the layer that matters. When both hold, the next thing on the roadmap is broader launch (Phase 2 triggers in `docs/launch-plan.md`).

This bar is deliberately qualitative. The signal at this stage is not engagement metrics or retention rates — it's *did testers see what we built, and did they see it the way we intended.*

## Roadmap (next sessions)

Sessions A+B+C of the Reflections surface (ADR 0007) shipped May 18 2026 — rename Log → Reflections, schema (`chapters` + `reflections`), weekly cycle-close ceremony, engagement-floor unlocks, locked-page. Per ADR 0008 (shipped 2026-05-20), the surface was then renamed to **Anchors**. **Anchors D / E / F shipped 2026-05-21** — journal + free-anchor create/edit/delete (D), `chapter_id` FK + archive-and-fresh-start (E, ADR 0009), and month/quarter/year ceremony routes via the unified `[cadence]` dynamic route (F). **Past chapters browse shipped 2026-05-21** — the read-only Settings surface that closes out ADR 0009's deferred item. **Submotions toggle + Split/Full mode shipped 2026-05-21.** **Inline swell editing shipped 2026-05-21.** **Wide-screen sidebar shell (Phase 1) shipped 2026-05-21** — multi-column per-page layouts deferred to future sessions. **Admin diagnostics at `/admin` shipped 2026-05-21.** **Anchor prompt bank shipped 2026-05-21** �� 25 MI-rooted prompts across 5 themes (notice / honor / consider / release / invite) replacing the 10 placeholders. **Wide-screen Phase 2 shipped 2026-05-21** — root layout widens to `lg:max-w-4xl`; proficiency view (`/swells/[id]`) gets a two-col grid at lg+ (sticky header + constellation left, motions list + waypoints right); Anchors page (`/anchors`) splits radar + ceremonies left, stats + breakdowns right; Settings flows sections into two columns. Mobile unchanged. Haptics investigation surfaced (but did not ship) that iOS Safari does not support the Vibration API — `navigator.vibrate(50)` silently no-ops on iPhone PWA; the code path is correct, the platform is the blocker. Honest "Not supported on this device" hint under the Haptic toggle shipped 2026-05-22 as part of the pre-tester checklist. **Wave layering polish shipped 2026-05-21** (testing-notes #16) — `WaveField` now draws 24 waves in 8 depth layers of 3; each wave fills below its path with bg (hides back-wave undersides), then a halo stroke clips at crossing points above, then the visible ink stroke draws on top; speeds scattered across layers without depth correlation so waves move at genuinely different rates. **Ombré daily progress bar shipped 2026-05-21** (testing-notes #18) — the flat `bg-th-btn` bar replaced with a `linear-gradient` from a washed-out `--th-accent` (35% in oklch mix with `--th-surface`) to full `--th-accent`; deepens as the day fills. **Pre-tester checklist shipped 2026-05-22** — haptics honesty hint, "Send feedback" mailto row, install instructions at `/welcome`, Sentry error monitoring. **Wake + Starter sets shipped 2026-05-22** — ADR 0010 (Wake: `lib/wakes.ts`, `<Wake>` component, locked-page wiring across all cadences, wave-mode hide/reappear, `WaveField` on cadence tiles, welcome-back fresh circle) and ADR 0011 (Build deflation: "Starter sets" rename, secondary-slot UI dropped, welcome-back copy reworded).

Backlog of small/mid items (polish, investigations, future features) lives at `docs/feedback/2026-05-20-testing-notes.md`. The Roadmap table below carries only the concrete next sessions.

| Session | Goal |
|---|---|
| ~~**Pre-tester checklist**~~ | **Shipped 2026-05-22.** Haptics honesty hint, "Send feedback" mailto row (`ondulertest@gmail.com`), install instructions at `/welcome`, Sentry error monitoring (`instrumentation.ts` + `instrumentation-client.ts` + `global-error.tsx`). |
| ~~**Wake + Starter sets**~~ | **Shipped 2026-05-22.** ADR 0010 (Wake): `lib/wakes.ts` polygon helpers + `generateRandomWake` for marketing, `<Wake>` component (full + mini modes, monochrome breathing outline, blurred wash), locked-page wiring across all four cadences with live actuals, coalesce-from-circle on blank slate, wave-mode hide-and-reappear (wake hidden during 72h+ wave, WaveField ocean replaces it), `WaveField` backdrop on month/quarter/year locked tiles, fresh pulsing circle on welcome-back with "Every motion has an impact" copy. ADR 0011 (Build deflation): "Your shape" → "Starter sets" in Settings + ShapePicker, secondary-slot UI dropped, welcome-back copy → "Pick up where you left off", link → "Try a different starter set →". |
| ~~**Privacy Policy + TOS + Cookie Policy**~~ | **Shipped 2026-05-23.** Privacy Policy via Termly (answers logged at `docs/legal/termly-privacy-answers.md`); Cookie Policy via Termly (scanner cleanup pass: removed `s7` false positive, added `tz_offset`, fixed Supabase cookie names); TOS hand-written (~2,400 words, 19 sections, at `docs/legal/onduler-tos-draft.md`) with Research Preview Status, Not Medical Advice, $100 liability floor, CA/Alameda County governing law, class action waiver. Identity baseline: Josh Jacobs personally (no LLC) DBA Onduler, `ondulertest@gmail.com`, (510) 479-0918, 201 University Ave Berkeley CA 94710. Routes: `/privacy`, `/terms`, `/cookies` (Termly HTML rendered client-side via `TermlyContent` component to avoid hydration mismatch from invalid Termly nesting), `/contact` (public form → `contact_submissions` Supabase table with anon-insert RLS, plus email fallback for FL/NE/TX two-methods compliance). Landing page gains footer links (Privacy \| Terms \| Cookies) and signup clickwrap ("By requesting an invite, you agree to our Terms and Privacy Policy"). Cookie Policy ships with Termly free-tier boilerplate intact — targeted-advertising / Flash-cookies / Cookie-Preference-Center sections are inaccurate but toggling them requires Termly Pro+ ($15/mo); deferred. |
| ~~**Marketing wake + landing page**~~ | **Shipped 2026-05-23.** `/api/wake-svg` endpoint for print-ready postcard SVGs (per-seed unique shapes, configurable size/colors/download). Landing page already shipped prior session with breathing wake hero, tagline, email capture, waitlist table. Dashboard already at `/dashboard`. |
| ~~**Three Whys swell setup**~~ | **Shipped 2026-05-23.** Resolved as guiding copy on creation forms instead of a multi-step flow — the three whys are already structurally embedded (motions = what you do, swells = what you feed, anchors = who you want to be). Motion creation: "Motions are verbs. Add actions that are important to you." Swell creation: "Swells are nouns. Your motions feed your swells. Add what you want to see in your life." Anchors ceremonies already cover the identity layer. Per testing-notes #30. |
| ~~**Perf audit**~~ | **Shipped 2026-05-23.** `getActiveChapterId` parallelized on all 4 pages that were calling it sequentially (swells, anchors, settings, proficiency view). Swells page collapsed from 3 sequential `Promise.all` batches to 2 (chapter + settings + timezone in first batch, all 11 data queries in second). Anchors page: chapter + timezone parallelized. Settings: chapter + settings parallelized, duplicate settings query removed. Proficiency view: chapter + non-chapter queries parallelized. Client-side audited: SwellRadar polygon math already memoized via `useMemo`, DailyChecklist uses `useTransition` for non-blocking mutations — no re-render issues found. N+1 query patterns: none detected. Per testing-notes #20. |
| ~~**Stripe plumbing**~~ | **Shipped 2026-05-23.** Crew product, 4 prices, webhook endpoint, checkout/portal actions, billing UI at `/settings/billing` (admin-gated). Full details in "Current state of the build" above. |
| ~~**Motions restructure (ADR 0013)**~~ | **Shipped 2026-05-24.** §1-2 (filter toolbar + popover) and §5 (checkbox drag-only) shipped first pass. §6 (duplicate motion from detail sheet with toast). §4 (cross-swell drag with toast undo + Keep both). §3 (Views week-edit mode): week grid with ombré day-boxes, last/this week toggle, optimistic logging, progress bar embedded in toolbar between calendar and filter icons, weekly progress tracking with real-time updates, all three filters (show completed, show swells, buckets) work in Views mode, week-complete auto-hide, date header shows "Week of Sunday, Month Day". Global toast infrastructure (`app/components/Toast.tsx`) wrapping root layout. |
| ~~**Wave state design**~~ | **Shipped 2026-05-25.** Unified wave field design language across wave surfaces: radar gets 24-line overlapping WaveField in wave mode (replaces static blue tint + wavy underline), Coming Together section shares one WaveField across all locked cadence tiles (circles removed). Independent radar wedges (no neighbor distortion), tangent axis labels. Also fixed timezone bug where local dev ran a day behind production. |
| ~~**Per-theme celebrations**~~ | **Shipped 2026-05-25.** Ripple (Tjørnuvík), tideline (Biarritz), bloom (Bolinas). Swell-crossing bloom on all themes. Old glow removed. |
| ~~**Polish batch (2026-05-25)**~~ | **Shipped 2026-05-25.** Bolinas dark earthy brown, Safari auto-zoom fix, filter popover backdrop, anchor period filter locking, prompt picker accordion, week boundary Sun→Sat alignment, 0% swell allocation visual cue, FrozenRadar visual update (matches SwellRadar). |
| ~~**Interaction model + celebrations + LLM import**~~ | **Shipped 2026-05-27.** Checkbox = log/unlog, row body = detail sheet, grip = drag handle (motions + swells). Celebrations unified to tideline-only across all themes (swell-colored ombré). LLM-assisted import with dual-mode parser (standard markdown + rendered copy-paste). |
| ~~**Install flow**~~ | **Shipped 2026-05-28.** ADR 0014. Three layers: (1) post-Personalize onboarding step with iOS share-button tutorial / Android `beforeinstallprompt` / desktop skip; (2) recurring dismissible dashboard tile with 14→30→never cooldown ladder; (3) Settings row renamed "Add to home screen" → "Install Onduler." `lib/install.ts` + `app/components/InstallInstructions.tsx`. No schema changes — all state in localStorage. |
| ~~**Engagement layer v1.1**~~ | **Shipped 2026-05-28.** Three moves from `docs/specs/engagement-layer.md`: A (compact live radar at dashboard top — `DashboardShape.tsx`, 140px, pulsing circle → fills on log, hides in wave, taps to `/anchors`), B (visible morph on every log — `onduler:log-committed` CustomEvent with weighted swell deltas, 250ms CSS transition on shape), G.1 (pre-unlock wake audit — confirmed live logs, no change needed). No schema changes. |
| ~~**Dashboard polish (2026-05-28)**~~ | **Shipped 2026-05-28.** Filter defaults flipped to show-all with "Hide X" labels. Hide wake toggle. Calendar view sticky nav (week label + nav buttons + day letters). Submotion row spacing. Wake: fixed-position, transparent on motions, frosted sea foam green (theme-blended) on calendar view. Anchors copy "swells" → "wakes". Removed "Go to motions" from `/welcome`. |
| ~~**Waypoint polish (2026-05-29)**~~ | **Shipped 2026-05-29.** Auto-save edit cards, click-outside/X close, motion link dropdown, target count cap (7 weekly / 31 monthly), one-shot bonus points, additive recurring hits with proportional ring + reset, fully dynamic radar scale (no fixed floor/ceiling). |
| ~~**Cleanup session (2026-05-31)**~~ | **Shipped 2026-05-31.** ESLint audit: 66 issues → 0 across 30 files. Removed unused imports/variables (closestCenter, logMotionOnDay, removeLogById, formatHrs, interpolatedWakePath, CENTER, dead newName assignment). Escaped 11 JSX entities. Fixed Unicode curly quotes in WavePrompt. Converted 3 localStorage reads to lazy `useState` initializers. Replaced `any` casts with proper types (`BeforeInstallPromptEvent` interface in `lib/install.ts`, typed navigator/window casts). Converted render-phase ref assignments to `useEffect`-based sync (SwellsList, MilestonesSection, MotionDetailSheet). Suppressed 18 legitimate `set-state-in-effect` false positives (DOM reads, prop sync) and 4 `purity` false positives (`Date.now()` in server components) with targeted `eslint-disable` comments. Added `varsIgnorePattern: "^_"` to ESLint config. TypeScript and production build clean. |
| ~~**Anchors + Journal v2 (2026-06-01)**~~ | **Shipped 2026-06-01.** Spec at `docs/specs/anchors-journal-v2.md`. `updateAnchor` (replaces `updateFreeAnchor`, supports ceremony edits), `updateAnchorField` save-on-blur, `getAnchorsForPeriod` paginated fetch, `getJournalData` weekly archive. `InlineAnchorLog` replaces Recent section on `/anchors`. Journal rewrite: all-collapsed weekly archive with wave/logs-only/anchors renderers. Per-ceremony radar scoping (month/quarter/year use own cycle window). Historical swell filtering by `created_at`. FrozenRadar rewrite (polygon-matching wedge slices). Cycle chip picker on `/anchors/new`. `+` on locked page. Wave detection counts anchors. |
| ~~**Hours format sweep (2026-06-01)**~~ | **Shipped 2026-06-01.** Move 6 from `docs/specs/behavior-change-layer.md`. All decimal-hours display replaced with HH:MM format (e.g. 0.75 → 0:45) across ~20 files. `formatHrs` in `lib/format.ts` rewritten; `parseHoursInput` added to `lib/periods.ts` (accepts both "0:30" and "0.5"). Hour inputs switched to `type="text" inputMode="decimal"`. Swell creation always writes both `target_points: 100` and `target_hours: 5` — no null targets on creation. `setTrackingMode` auto-populates `target_hours` from `target_points` on mode switch as safety net. Both `points` and `hours` recorded on every log row regardless of mode. Wake and actuals polygons get 12% minimum radius floor so zero-value swells show a rounded stub instead of collapsing to center. "Show hrs/pts" toggle added to swells page eye filter. |
| ~~**Behavior change layer (2026-06-01)**~~ | **Shipped 2026-06-01.** Moves 1, 2, 3, 5 from `docs/specs/behavior-change-layer.md`. Move 1: intensity picker (long-press checkbox → light/medium/deep popover with live pts/hrs, multiplied at log time). Move 2: intentions in ceremony (observation + intention merged, prior intention as next-week placeholder). Move 3: friction audit (bucket seed chips, contextual copy, above-fold confirmed on iPhone). Move 5: bucket copy edit ("Cluster motions by when they happen"). Shared constants in `lib/intensity.ts`. Personalization: background photo upload with pinch-to-zoom + drag-to-reposition, client-side compression, frosted-glass sticky headers, custom progress bar color. iOS Safari compat via fixed-position DOM element. Move 4 (share sheet / social proof) deferred to native phase. |
| **Anchors refinement + aesthetic batch (partially shipped 2026-06-05)** | Brainstorm over `docs/aesthetic-sensibility.md` produced four specs. **Shipped 2026-06-05:** the Anchors refinement spec (`docs/specs/anchors-refinement-2026-06-05.md`, all four moves); the horizontal-overflow fix via the sticky frosted pill rewrite (see Current state), which made the legibility spec's `overflow-x` workaround unnecessary and replaced the full-bleed frost with a contained rounded pill across every sticky header; and the floating pill nav spec (`docs/specs/bottom-nav-floating-pill-2026-06-05.md`) plus matching floating top treatment, pin/unpin toggle, photo-frost on the bottom pill, and Settings header reorganization (see Current state). **Pinned for noodling:** photo-legibility text-shadow approach. The amended spec (single dark shadow both modes, drop white-halo light branch) was tried and reverted same session — the dark-shadow-on-darkened-photo combo adds no contrast in dark mode, and the original white-halo blurs colored text on bright photos in light mode. Both modes share the root problem that the shadow color matches the dominant tonality on either side, so it can't separate text from background. Idea to revisit: use the dark-mode shadow approach in light mode too (Josh, 2026-06-05) — confirm contrast logic in a focused session before committing. **Still queued:** photographer anchor prompts (`docs/specs/anchor-prompts-photographer-2026-06-05.md`), wake-export-print (`docs/specs/wake-export-print-2026-06-05.md`). |
| **Next** | Premium feature gating + remove admin gate on Crew (paused until testing phase clears v1 exit criteria). |
| **After next** | App integrations — meditation / cooking / exercise app completions auto-log to Onduler. Write the spec first at `docs/specs/app-integrations.md` (OAuth + webhook shape, mapping from external completions to motions, conflict handling), then schedule implementation. Per testing-notes #3 / #29. |
| **Operator-blind content encryption (spec drafted 2026-06-17)** | Make content fields (motion/swell/group/milestone names + anchor journal text) unreadable by the operator via client-side E2EE — "even I can't read your journal." Spec at `docs/specs/private-content-encryption.md`: field-level AES-GCM, multi-slot envelope (passkey-primary via WebAuthn `prf`, recovery-code backup, password fallback), deferred low-friction onboarding, lazy client-side migration of existing rows. Cheapest to do now while data is small (same logic as DB-hardening), ideally before opening to strangers. **Prerequisite spike PASSED in full (2026-06-17):** WebAuthn `prf` returns a stable secret on iPhone iOS 26 + desktop, AND the iCloud-synced passkey yields a byte-identical secret across iPhone and Mac Safari — cross-device backup confirmed. Passkey-primary locked; throwaway test page removed post-verification. **Phase 1 SHIPPED 2026-06-17:** `lib/crypto/content.ts` (Web Crypto field encrypt/decrypt + multi-slot envelope, 11 vitest tests, `npm test`) and key-slot schema (`scripts/migrate-content-encryption.sql` applied to prod — `user_key_passkeys` table + `enc_*` columns on `user_settings`). No behavior change yet. **Phase 2 SHIPPED 2026-06-18 (key lifecycle + slots at auth; still NO content encrypted — `enc_enabled` stays false):** **Tiered, passkey-first model** (decided this session, supersedes the spec's "wrap all three at signup"): Tier 1 passkey + recovery (preferred, no password slot); Tier 2 adds optional password fallback; Tier 3 password + recovery for passkey-unsupported devices. Recovery code is **always mandatory**; the **password slot is a spare tire** — created only when (1) passkey unsupported, (2) passkey ceremony fails + user chooses fallback, or (3) explicit Settings opt-in — and derives from the **account password** (prompted at that moment; not captured transparently). No separate app passcode. **Email confirmation turned ON this session** (supersedes the "OFF for wave 1" decision) — so key setup runs at the **post-confirm session**, not signup: `signUp` now sends `emailRedirectTo=/auth/callback?next=/protect` and the "then sign in" copy dropped from the check-email screen. New files: `lib/crypto/keys.ts` (DEK build, slot wrap/unwrap for passkey/recovery/password, Crockford recovery-code gen/canonicalize, WebAuthn-PRF register/unlock + `passkeySupported`/typed errors), `lib/crypto/dek-store.ts` (in-memory extractable + IndexedDB non-extractable DEK cache, cleared on sign-out), `lib/crypto/keys.test.ts` (11 tests — 22 total pass), `app/actions/keys.ts` (RLS-scoped persist/read of WRAPPED blobs + salts only — `getEncSetupState`/`getKeyEnvelope`/`persistCoreSlots`/`persistPasskeySlot`/`persistPasswordSlot`/`remove*`), `app/components/DekProvider.tsx` (`useDek()` context, mounted in root layout, hydrates from cache + clears on Supabase `SIGNED_OUT`), `app/protect/page.tsx` + `ProtectFlow.tsx` (the privacy gate: setup = passkey-first → mandatory hard-block recovery-code download/copy + type-back → optional password fallback; unlock flow built but latent until Phase 4). **Gating:** `/onboarding` redirects to `/protect` when `getEncSetupState().complete` is false (recovery + ≥1 primary), so no content flow happens before keys exist for new users; already-onboarded users short-circuit so existing testers aren't trapped (their migration is Phase 6). `/protect` added to `proxy.ts` protected routes. `content.ts` gained `kekFromBytes` + exported b64url codecs. **Known gaps deferred to step 7 (slot management):** `changePassword` does NOT yet re-wrap the password slot (a Tier 2/3 user who changes their password must then unlock via passkey/recovery until re-wrap ships); Settings slot-management UI (add passkey, enable/remove password fallback) not built; bailing mid-setup can leave an orphan WebAuthn credential on-device (harmless — nothing persists server-side until recovery is confirmed). **Phase 2 flow redesign (2026-06-18, same session):** moved key setup OFF a post-confirm `/protect` round-trip and INLINE into signup, by switching email confirmation from a **magic link to a 6-digit OTP code** (`{{ .Token }}`). Rationale: the code keeps the user on the original signup page (fixes the messy multiple-magic-link-tabs problem AND lets them confirm on any device), and critically the just-typed account password stays in memory — so the password-fallback slot reuses it with **no second password entry**. New signup flow: `email+password → enter 6-digit code (verifyOtp client-side, type:'signup') → inline KeySetup → /onboarding`. **Recovery-code policy softened** (decided this session): required (save + type-back) **only on the password path**; passkey users get the code offered as a download with a plain Continue (their synced passkey is already a cross-device backup) — because in E2EE a password reset does NOT recover the DEK, so the code matters most for password-only users. Shared **`app/components/KeySetup.tsx`** now owns the whole setup state machine (passkey-first → password fallback reusing an in-memory `password` prop if present, else asking → conditional recovery step); both `app/signup/page.tsx` (with password) and `app/protect/ProtectFlow.tsx` setup mode (without) render it, so they can't drift. `/protect` keeps the unlock flow for returning users. Merged the old intro + passkey screens into one ("Protect what you write" → "Choose your passkey"); single-field password re-entry (no confirm); fixed a hydration mismatch (passkey support detected post-mount, `supported: boolean|null`). **Requires a Supabase dashboard change (Josh, not in repo): Authentication → Emails → Confirm signup template must surface `{{ .Token }}` (the 6-digit code) instead of relying on `{{ .ConfirmationURL }}`.** Passkey unlocks CONTENT (via PRF), not the account — account auth stays email/OTP; merging passkey into sign-in is a separate future Supabase Auth feature. **Phase 3 SHIPPED 2026-06-18 (write paths — still INERT: gated on `enc_enabled`, false for everyone, so writes stay plaintext until migration):** client-side encryption of content fields before every create/update server-action call. New `app/components/useContentCrypto.ts` hook (`encryptContent(value)`, `encryptFormData(fd, fields)`, `decryptContent()` for Phase 4) gated on `encActive = dek && enc_enabled`; empties pass through so required/nullable checks survive. `DekProvider` now exposes `enc_enabled` (fetched via new `getEncEnabled()` action only when a DEK exists, so public pages pay nothing). Pattern: FormData forms wrap the action in a client fn that `encryptFormData(fd, ['name'])` then calls the server action (JSX unchanged); direct calls do `await encryptContent(name)` at the call site. Wired across **groups** (AddGroupForm, EditGroupForm, createQuickGroup), **motions** (AddMotionForm/createMotion, MotionDetailSheet updateMotionDirect ×3 + updateSubmotionDirect + createSubmotion, DailyChecklist), **swells** (AddSwellForm/createSwell, SwellProficiencyView updateSwellDirect), **milestones** (AddMilestoneForm createMilestone, MilestoneRowName renameMilestone, CadenceSection createMilestone), **reflections** (NewAnchorForm createFreeAnchor, EditAnchorForm updateAnchor, InlineAnchorLog updateAnchorField, CycleCeremony saveReflection ×4 via a shared `reflectionInput()` helper). `duplicateMotion` gained an optional pre-built `name` param (client builds the encrypted "{name} (copy)" from the displayed name; server falls back when absent). The `updateMotion`/`updateSwell` FormData actions are confirmed **dead** (no callers) — left untouched. **Deferred — the import write path (`confirmImport`)** is the one piece not done: its whole entity-linking (name dedup, junctions, group/swell mapping) is keyed on plaintext names, and the correct fix (client-side dedup) needs to DECRYPT existing names — a Phase 4 read capability. Safe to defer (works as-is while `enc_enabled` is false); must be refactored alongside Phase 4 before activation. **Phase 4 SHIPPED 2026-06-18 (read paths + feature fixes):** decrypt content client-side at render boundaries (journal, archived chapters, dashboards, settings); client-side export + restore safety net (v3, all chapters); **import dedup refactored** (`getImportEntities` → client-side `resolveImport` against decrypted names → blind `confirmImport`, `lib/import-resolve.ts` + tests) — this closed the Phase-3-deferred import piece; admin user view blinded (`isEncrypted` → "🔒 encrypted"). **Phase 6 (migration) SHIPPED 2026-06-19 — encryption is now LIVE per user:** the one-time, lazy, client-side migration that finally flips `enc_enabled`. New `lib/crypto/migrate.ts` (PURE, testable: `buildMigrationUpdates`/`hasMigrationWork` — encrypt each content field only when it's a non-empty string that ISN'T already `enc:`-prefixed, so re-runs never double-encrypt; `lib/crypto/migrate.test.ts`, 4 tests / 46 total), `app/actions/migrate.ts` (`getContentForMigration` reads all content rows across **all chapters** incl. archived; `writeMigratedContent` blind per-row updates; `setEncEnabled` flips the flag **last**, guarded to refuse unless recovery + ≥1 primary slot exist so a user can never be stranded in ciphertext mode with no key), `app/protect/runMigration.ts` (orchestration: fetch → encrypt → write → flip). `ProtectFlow` restructured into a 3-step machine — obtain DEK (setup **or** unlock, both leave it in `DekProvider`) → if already `enc_enabled` just continue → else run a "Securing your content" step that migrates then redirects; the old "already-unlocked → straight through" shortcut is gated so it can't skip migration. `DekProvider` gained `refreshEncEnabled()` so write/read paths activate post-migration without a full reload. **Trigger gate (page-level, `proxy.ts`/auth untouched):** onboarded users with `enc_enabled=false` are redirected to `/protect` — inlined in `/dashboard` (already loads settings) and via new `lib/enc-gate.ts → gateContentEncryption()` on `/swells`, `/anchors`, `/settings`. Handles both populations uniformly: new users (keys already exist, DEK cached) migrate their freshly-created onboarding content in a brief auto-step; existing keyless testers set up keys via `KeySetup` (passkey, or account-password fallback — no dashboard config needed) then migrate. Idempotent + crash-safe: a partial migration leaves `enc_enabled=false`, re-gates, and resumes skipping already-encrypted rows. **MERGED TO `main` + DEPLOYED TO PRODUCTION 2026-06-19** (PR #1 squash-merged → `onduler.app`). Both manual Supabase dashboard prereqs are now DONE: passkey **RP ID = `onduler.app`** and the `{{ .Token }}` OTP confirm template. **Verified live 2026-06-19:** passkey signup + unlock works on `onduler.app`, and the per-account migration flips `enc_enabled` with content landing as `enc:v1:…` ciphertext. Two bugs were found + fixed during the live test: a `/protect` hydration mismatch (`DekProvider` persists across the dashboard→protect redirect, so its `loading` differed server vs client — fixed with a mount flag) and a StrictMode cleanup that cancelled the only migration run's redirect, stranding the "Securing" screen (dropped the active/cleanup flag, relying on the once-guard). Ops note: `is_admin` has **no email-based auto-grant** — deleting + recreating the `ondulertest` account resets it to the `false` default, so it was re-granted via a one-line SQL `update user_settings set is_admin = true`. **Step 7 part 1 SHIPPED 2026-06-19:** `changePassword` password-slot re-wrap (Settings path — re-wraps the password slot under the new password client-side after a logged-in change) + Settings → Privacy slot-management UI (`app/settings/KeyManagement.tsx`: add/remove passkey, enable/remove password fallback, with a "never drop below one primary unlock" guard; reuses existing actions, no schema change). See the Current state entry. **Step 7 part 2 SHIPPED 2026-06-19 (locked-out reset + recovery regeneration; encryption work is now COMPLETE):** the `/reset-password` forgot-password path is now content-encryption aware. Since that page is reached via the reset email with a session but **no DEK in memory**, setting a new password would otherwise strand the password content-slot (still wrapped under the forgotten old password). The page now (1) warns *before* the reset that a new password alone doesn't unlock private notes, and (2) *after* the reset, if a password slot exists (`getEncSetupState().hasPassword`), runs a "Re-open your private content" step where the user unlocks the DEK with **passkey or recovery code** (the password option is hidden there — it's the slot being repaired), then re-wraps the password slot under the new password (`addPasswordSlot` → `persistPasswordSlot`) and caches the DEK for the session. A passkey-only user (no password slot) skips straight through. Truly-locked-out users (no passkey + no recovery code) get an honest "your old notes stay locked, not even we can open them — you can still sign in and unlock later" escape rather than an inline data-wipe (true fresh-start-with-new-DEK stays deferred — destructive and rare). The passkey/password/recovery unlock UI was extracted from `ProtectFlow` into a shared **`app/components/UnlockPanel.tsx`** (`onUnlock(dek)` callback; the recovery-code path makes it the **recovery-only lockout escape**), now used by both `/protect` and `/reset-password` so they can't drift. **Recovery-code regeneration** added to Settings → Privacy (`KeyManagement.tsx`): `buildRecoverySlot(dek)` in `lib/crypto/keys.ts` mints a fresh code + slot client-side; the new code shows with download/copy and is only persisted (via new `persistRecoverySlot` action — recovery columns only) once the user confirms "I saved it", so the **old code stays valid until the new one is saved**. No schema change. tsc/eslint/build clean, 26 crypto unit tests pass. **Reset flow converted to OTP (2026-06-19, same session):** the first live forgot-password test exposed that **link-based PKCE resets are broken** — the magic link's `redirect_to` was falling back to a stale Supabase **Site URL = `http://localhost:3000`** (fixed: Site URL set to `https://onduler.app`, both `…/auth/callback` URLs allow-listed), and even with the link correct, PKCE binds the code-verifier to the requesting browser's cookie jar, so tapping the link in a phone mail app's in-app webview lands the user on `/login` (failed `exchangeCodeForSession`). Fix: `/forgot-password` is now a **code (OTP) flow** mirroring signup — email → `resetPasswordForEmail` (no `redirectTo`) → enter the emailed code → `verifyOtp({ type: 'recovery' })` sets a session client-side → navigate to `/reset-password` (the existing set-password + re-secure flow, unchanged). No link, no `/auth/callback`, no PKCE, works cross-device. **Requires a Supabase dashboard change (Josh, not in repo): Authentication → Emails → Reset Password template must surface `{{ .Token }}` (the OTP) instead of the `{{ .ConfirmationURL }}` link.** OTP length follows the project's "Email OTP Length" setting (currently 8). **Auto-unlock gate (2026-06-19, same session):** closed the lock-discovery gap surfaced live — a logged-in user whose content is encrypted (`enc_enabled=true`) but who has no DEK on this device (the reset-on-a-fresh-device case) saw blank motions/anchors with no prompt, because the only redirect to `/protect` fires when content is NOT yet encrypted. New `app/components/UnlockGate.tsx`: a client overlay mounted once in the root layout (inside `DekProvider`) that, when `encEnabled && !dek && !loading` and not on an auth/setup route (`/protect`, `/login`, `/signup`, `/forgot-password`, `/reset-password`), shows an "Unlock your content" overlay using the shared `UnlockPanel` (passkey/recovery/password); on unlock the DEK lands in `DekProvider`, the overlay unmounts, and content decrypts live with no navigation. To make a keyless-but-encrypted session detectable, `DekProvider` now also reads `enc_enabled` when there's no cached DEK — guarded by a LOCAL `getSession()` so public/logged-out pages still pay nothing (getSession is local; `getEncEnabled` runs only when a session exists). **DEK cache is now user-scoped (2026-06-19, same session — bug found in local testing):** the IndexedDB DEK cache (`lib/crypto/dek-store.ts`) was a single global `current` record cleared only on `SIGNED_OUT`, which the OTP reset path never fires. A stale/foreign key from a prior account on the device (e.g. the deleted+recreated `ondulertest`) was served by `getDek()`, so `dek` looked present → UnlockGate never fired, `/protect` bounced back to the dashboard via its already-unlocked path, and the wrong key decrypted to blank content — all three symptoms at once. Fix: the cached record now stores `{ key, userId }`; `getDek(userId)` returns it only on a userId match and purges any mismatched or legacy (unkeyed) record, so a wrong key can never masquerade as unlocked. `setDek(dek, userId)` and `DekProvider` resolve the session user id (local `getSession`) to scope reads/writes; `onAuthStateChange` keeps the id current. **Always-on password fallback (2026-06-19, same session — REVERSES the Phase-2 "password slot is a spare tire" decision):** every new signup now gets a password slot wrapped under the account password, even on the passkey-success path — `KeySetup.tryPasskey` builds it whenever the account password is in memory (the signup path always has it). Rationale (Josh's call, lockout concern): a user who knows their password should always be able to unlock on a new device, so a lost/un-synced passkey + unsaved recovery code never means total lockout. Accepts the honest-operator caveat for that slot (spec line 58). The `/protect` adoption path for already-onboarded keyless testers still has no in-memory password, so those few set up passkey+recovery and can enable password in Settings — new users (the universal OTP signup path) are all covered. Note this does NOT make a forgotten password recoverable: a password *reset* can't unwrap the DEK (sealed under the old password) without a passkey/recovery unlock — it only helps users who still KNOW their password. **"Start fresh" lockout escape (2026-06-19, same session):** for the genuine dead-end (forgot password AND no passkey AND no saved code — unrecoverable by anyone, the E2EE guarantee), a door out of the unlock screen. New `startFreshAfterLockout()` server action (`app/actions/keys.ts`): deletes all chapters (cascades every chapter-scoped table + children), drops all `user_key_passkeys`, and resets `user_settings` encryption + onboarding flags to brand-new (keeping plain prefs) — DESTRUCTIVE/irreversible, RLS-scoped. New `app/components/StartFreshEscape.tsx`: a buried two-step "I've lost my passkey, password, and recovery code" → blunt copy → "Permanently delete and start fresh" (red) that runs the wipe, clears the DEK, refreshes enc state, and routes to `/protect` for fresh key setup; shown in both the `UnlockGate` overlay and `/protect`'s unlock flow. **Both prerequisites are now DONE (2026-06-19): Josh changed the Reset Password template to surface `{{ .Token }}` (OTP) this session, and `main` is deployed to prod. The only thing left is the live end-to-end test itself — run a forgot-password OTP reset and the "Start fresh" escape on `onduler.app`.** |

## Decisions

### Groups vs Swells (May 2026)

Groups and Swells solve non-overlapping problems and should not be collapsed into one concept.

- **Groups** are organizational containers. One motion belongs to one group (or none); one swell belongs to one group (or none). Pure UX utility — they help the user mentally cluster and filter their lists. They answer the question *where does this live in my list?*
- **Swells** are aspirational goals. A motion can contribute to many swells; a swell is fed by many motions. Each motion→swell link carries its own `contribution_weight`, enabling the additive points model (e.g., meditation can contribute 100% to "be healthy" and 40% to "be a better partner" simultaneously). They answer the question *why does this matter?*

Locked schema target:

```
groups        (id, user_id, name, sort_order)
motions       (id, user_id, group_id NULLABLE, name, base_points, ...)
swells        (id, user_id, group_id NULLABLE, name, target, ...)
motion_swells (motion_id, swell_id, contribution_weight)
```

`group_id` on motions is nullable — ungrouped motions are fine, especially early on when a user is still figuring out their categories.

**Why this matters.** Most habit apps either have flat lists (no organization) or single-goal tagging (no additive credit). Collapsing Groups and Swells would either lose the additive points benefit (if everything became 1:many) or make the motion list unscannable (if everything became many:many). The two-concept model is Onduler's distinctive structural choice — don't propose simplifying it away.

**Implementation gap (as of May 2026).** Schema now matches the locked target: `motions.group_id` FK is live, and `motion_swells.contribution_weight` is active in all aggregation paths (swells page, log page, swell progress bars). The old `motion_groups` junction is gone.

### Groups on swells (May 2026)

Groups now apply to swells the same way they apply to motions. Each swell belongs to one group (or none); the `groups` table is shared across motions and swells. A single "Health" group can have both motions and swells pointing at it.

**Why.** Same folder logic as motions — "where does this live in my list?" — applied to swells. Two user modes drop out of the same shape:

- The user who wants **mirrored organization** between motions and swells uses the same groups on both sides. Filtering by a group on the motions page and navigating to swells keeps the filter, and the corresponding swells appear. Feels consistent.
- The user who wants **independent organization** assigns groups to motions and swells separately, with little or no overlap. Filtering on one side surfaces nothing on the other, which correctly reflects that the group isn't used there. Feels independent.

The misalignment diagnostic (does my daily motion clustering match my long-term swell clustering?) remains visible at the group level without requiring M:N or separate group tables.

**Locked schema for the extension:**

```
swells (id, user_id, ..., group_id NULLABLE FK → groups, ...)
```

No `swell_groups` table. No `motion_groups` junction (already removed). Single shared `groups` table.

**Shipped (May 2026).** `swells.group_id` FK is live. UI wired: group assignment on swell detail, grouped display on swells page, group filter. ADR: `docs/decisions/0001-groups-schema.md`.

### Swells repeat weekly, Sunday-anchored (May 2026)

All swells run on a single weekly cycle anchored to Sunday. The old "set a lifetime target, fire one celebration, done" mode is removed. A swell's target is now a weekly target; celebration fires when cumulative weekly progress crosses it; the cycle runs Sunday 12:00 AM → Saturday 11:59 PM Pacific (Sun→Sat), and the counter starts at zero again on Sunday morning.

**Why.** Swells are not destinations. They are philosophies the user wants their life to feel full of — creativity, beauty, connection, freedom, good food — which require steady weekly feeding, not one-time completion. The one-shot-celebration framing implied that life-want goals get *finished*, which is the wrong frame and the wrong product. A weekly cycle matches how lives are already structured (work week / weekend), gives the diagnostic "which want am I feeding most?" a stable reference window, and pairs philosophically with the wave/tide vocabulary — Onduler is about rhythms.

**Sunday anchor.** The week starts Sunday morning — ceremonies unlock then if they qualify, and the new cycle is ready the moment the user opens the app on Sunday. Most users won't notice until sometime during Sunday; the app doesn't push a notification, it waits for the user to come when they're ready to be reflective. A missed week is not punishment; it's a wave, and the app's posture stays "we'll be here when you're back."

**Motions can stay unassigned.** A motion does not have to feed a swell. The gap — motions you do daily that don't feed any stated want — is itself a useful diagnostic surface: the user either adds a swell to cover it, or asks whether the motion belongs in their daily life at all. Future feature, not in scope for the weekly-model session.

**Onboarding implication.** Swell-first, then motions per swell. The swell step is a noun-shaped menu of common life areas with custom add, replacing the current quick-start / build-your-own flow. Listing swells is required to engage with the app. The noun/verb framing surfaces explicitly in onboarding copy: "Swells are nouns. Motions are verbs." See ADR 0002. The onboarding rework is its own session after the weekly-model session lands.

**Canonical swell menu (10).** The seeded menu in onboarding is:

| Swell | Description |
|---|---|
| Movement | exercise, sport, walking, being in your body |
| Mind | meditation, journaling, reading, learning |
| Nutrition | cooking, eating well, nourishment |
| Home | your space, comfort, domestic life |
| Family | partner, kids, parents, siblings |
| Friends | broader social life, community |
| Work | your livelihood, career, craft as profession |
| Money | finances, savings, side income |
| Creativity | making things, art, music, writing |
| Adventure | travel, novelty, trying new things |

The user can rename, recolor, deselect, or add custom swells. Descriptions are shown as sub-text under the swell name in the menu and disappear once the swell is picked.

**Schema impact.** `swells.target_points` and `swells.target_hours` now mean "per week." No `repeats` flag — one mode only. Cycle state is derived from `logs.logged_at` and the Sun→Sat week definition, not stored on the swell. Lifetime totals can still be shown as a stat but do not drive celebration.

**Locked defaults.** ~~100 pts/week or 5 hrs/week~~ Superseded 2026-06-12 by motion-derived targets: where a swell's feeding motions are known at creation (onboarding, AI import), target = 4 logs/wk of each feeding motion in points mode (`ceil(4 × Σ pts×weight)`), 3 logs/wk in hours mode (0.25-hr ceil). The floor (a motionless or bare swell) is 4 pts / 3 hrs, matching what one default motion yields — corrected 2026-06-12 from 12 pts, which had a swell with no motions outranking a 1-motion swell (target 12 vs 4) on the radar for no visible reason; the hours floor was already consistent. Bare creation (manual createSwell, starter sets) shares the same 4 pts / 3 hrs floor. This resolves the ADR 0003 "defaults likely re-tune" note — the normalized contribution model plus the 1–3 pt scale made 100/wk unreachable (a 3-motion swell maxed ~42/wk), so celebrations never fired.

**Migration.** None required. Single-user (Josh), one week of data, no production users. Old swell targets get reinterpreted as weekly when the session ships.

### Swells are noun-shaped life areas, motions are verb-shaped actions (May 2026)

The mental model for users and copy is: **Swells are nouns. Motions are verbs.** A Swell is a noun-shaped area of life the user wants to invest in — Movement, Home, Food, Family, Creativity, Reflection, Work, Travel, and so on. A Motion is a verb-shaped daily action — kayak, cook, decorate, journal, make a playlist. The previous framing of Swells as "philosophies the user wants their life to feel full of" was poetically correct but freeze-inducing in practice; the noun/verb pattern unsticks users in one sentence.

**No schema change.** A Swell is still a row with a name and a weekly target. The reframing is in onboarding copy, default suggestions, and UI hints — not in the data model.

**Onboarding shift.** The swell step becomes a pick-then-add menu of common life areas with custom entry as a follow-up. The previously-planned open-ended "What do you want your life to feel like?" prompt is replaced. Motions are then added per swell.

**Contribution-weight default.** New motion→swell links default to `contribution_weight = 100`. The split-percentage affordance remains in the motion detail sheet but is not surfaced until the user has at least one link saved. In the noun-shape model, most users assign one swell per motion at full weight; weighted multi-swell contributions become an enrichment for users who want them, not a load-bearing requirement. The additive interpretation of `contribution_weight` (a motion's contributions across swells can exceed 100% total) is superseded by ADR 0003 — see the Normalized contribution model decision below.

**Vocabulary unchanged.** The user-facing word stays "Swell." "Domains" remains on the never-use list. The noun-shape framing is an internal teaching pattern, not a rename.

ADR: `docs/decisions/0002-swells-are-nouns.md`.

### Normalized contribution model (May 2026)

A motion's contributions across all the swells it feeds sum to ≤100% (not >100%, as the original additive model permitted). A motion is worth exactly its base value, distributed across its swells — never multiplied. Adding a second swell to a motion opens an allocation slider; editing one swell's allocation auto-rebalances the others on that same motion, proportionally, to keep the total constant. The motion detail sheet surfaces a "X% remaining" indicator.

**Why.** The additive model dispensed more credit than the action contained (a 3-pt meditation could contribute 100% to Mind and 40% to Family, totaling 4.2 pts of swell credit for one 3-pt action). The math was generous but conceptually fuzzy, making "where did my points go this week?" un-answerable and weekly targets squishy. The normalized model gives targets absolute meaning and aligns the math with how users actually describe their actions (a split, not a duplication). It also gives build-suggested motion splits (ADR 0004) stable meaning — "Yoga → 60% Movement, 40% Mind" composes predictably when shapes blend.

**Schema unchanged.** `motion_swells.contribution_weight` keeps its column shape and meaning. Constraint is app-enforced. One-time renormalization of existing rows on ship: any motion whose links sum to >100% gets its weights scaled to sum to 100%, preserving proportions. Acceptable because there's one user and one week of real data.

**Defaults likely re-tune.** Weekly target defaults (currently 100 pts/week or 5 hrs/week) probably want to drop after a few weeks of normalized data — less credit flows per motion, so targets become harder to hit at the current numbers. Deferred until there's data to look at.

ADR: `docs/decisions/0003-normalized-contribution-model.md`.

### Builds and the proficiency view (May 2026)

Onduler adopts an RPG-skill-tree-influenced model for orienting a user's week, internally called a **build** and user-facing as a **shape** (as in "a maker's rhythm," "your shape," "try a different shape"). Internal/external vocabulary split — RPG framing stays in design and code identifiers; surf voice stays at the surface. Builds are *suggestions, never prescriptions*: they curate the menu of what counts (suggested motions, suggested swells, suggested waypoint templates) but never weight some motions more than others.

**Four preset shapes** ship as placeholders: The Maker (Creativity, Work, Mind), The Athlete (Movement, Nutrition, Mind), The Wanderer (Adventure, Movement, Creativity), The Scholar (Mind, Work, Creativity). Caretaker is dropped pending naming pre-launch. Users always retain the "build your own shape" path.

**Onboarding stays as-is** — the existing light 2x2 pack picker is the right shape for an empty starting state. The richer build experience (gallery + diff/preview + per-change opt-out + secondary-shape slot + custom path) lives in **Settings**. Tapping a preset opens a preview showing exactly what would change (swells to add, swells to hide, targets to retune); each change is individually opt-out-able. Nothing is deleted on respec; swells are hidden at most. Logs and motions are sacred.

**Swells page rebuilds as per-swell proficiency views** — the visually heaviest move and the v1 deliverable. Each swell becomes a header (weekly tide bar + small lifetime stat, no tier badges) plus a constellation of motion nodes (size = contribution weight, opacity = recent activity, log count inside), arrangeable via drag, capped at 8 with a "+ N more" overflow to a flat list. List-view toggle in the top-right. Time-view toggle (Week / Month / Lifetime). Waypoints section below — recurring on top, one-shots below, sortable, completed collapse by default.

**Today page stays sacred.** Paper-list aesthetic, flat motion checklist, simple, customizable, welcoming. All structural complexity lives on Swells and Settings. Today is a daily ritual surface; Swells is a strategic surface; the two-surface split is how Onduler scales without becoming exhausting.

**Waypoints** (table stays `milestones`, see ADR 0006) are user-authored, two flavors: recurring (cadence-based, partial-ring visual, hitting cadence celebrates and adds bonus points) and one-shot (discrete goal, marked off manually, optionally shows a small progress crumb from a linked motion, becomes a permanent marker on the swell's proficiency view when completed). Builds may suggest waypoint templates; the user always writes the specifics.

**Wave / build interaction.** When the user is on or returning from a wave, the build is at rest, not erased. Logs-page lens defaults to a softened view (wave-aware targets, "hitting these still counts") with a Softened/Reference toggle. Welcome-back screen after WaveGrid check-in offers two doors — *Ease back in* (recommended, soft 3-week ramp 40% → 70% → full) and *Pick up your [shape]* (full targets) — plus a small "Try a different shape" link. Per-shape minimum-viable-shape anchors are auto-selected from the user's top 2 most-logged motions, with Settings affordance to drop, swap, or add.

**Schema additions:** `motion_swells.position_x/y` for arrangeable constellation layout; new `milestones` table (user-facing name: Waypoint — kind: recurring | one_shot, optional cadence / target_count / completed_at, bonus_points, sort_order); `user_settings.primary_build` and `user_settings.secondary_build`. Secondary slot ships as UI only in v1; multi-classing math lands with the Logs page redesign.

ADR: `docs/decisions/0004-builds-and-the-proficiency-view.md` (amended by ADR 0006 for user-facing waypoint rename).

### Calendar-month monthly cycle, wave month, and ceil display rule (May 2026)

The Log page and proficiency view gain a calendar-month view alongside the existing weekly view. Monthly cycle anchors to the calendar month (first-of-month reset), running independently alongside the Sunday-anchored weekly cycle. Monthly target is never stored — derived at render via `ceil(weekly × days_in_month / 7)`. Lifetime target similarly derives via `ceil(weekly × weeks_since_first_log)`.

**Wave month** triggers iff there's a run of 7+ consecutive zero-log days inside the calendar month. Less than 7 doesn't trigger the wash. The threshold has a designed side effect: a user who logs even once per Sunday-to-Saturday window never trips wave-month — that's the gentle weekly check-in nudge built into the math.

**Symmetric drag editability:** weekly target stays the integer source of truth (drives the celebration anchor). Drag on the monthly view edits the underlying weekly target with appropriate step scaling. Live pill on monthly view shows both values (`Mind · 222 pts/mo · 50 pts/wk`). Lifetime view is read-only — recognition surface, not tuning surface.

**Ceil everywhere a number reaches the screen** (see corresponding working agreement). Precision under the hood, integer display on top.

Filter labels rename: `7 days` → `This week`, `30 days` → `This month`. Schema unchanged.

ADR: `docs/decisions/0005-monthly-cycle-and-wave-month.md`.

### Waypoint (user-facing rename) and export labels (May 2026)

The user-authored marker-within-a-swell previously called "Milestone" is renamed **Waypoint** in all user-facing surfaces. The schema table remains `milestones` and all code identifiers stay — same internal/external split already used for `builds` → user-facing "shape." The rename is a UI-copy change, not a refactor; ADR 0004's schema, behavior, and design rationale are unchanged.

**Why waypoint.** "Milestone" was the one user-facing term that didn't live in the ocean — Tide/Wave/Swell/Motion are water, milestone is highway construction. Waypoint is nautical/navigational without being surf jargon (most users know it from GPS, hiking, boating), so it fits the metaphor family without expanding the niche-vocabulary load the way "buoy" or "marker buoy" would. Semantically it carries the journey-marker meaning the entity already has. The mild tension with the swells-aren't-destinations doctrine is resolved by reading waypoints as *intermediate markers within an ongoing rhythm*, not endpoints of it. The locked surf vocabulary expands from four terms (Tide/Wave/Swell/Motion) to five (adding Waypoint).

**Export labels.** Data exports (the existing `/api/export` JSON download and any future export surface) label data with the words the user sees in the UI — Waypoint, Swell, Motion, Group, Tide, Wave — not the internal table names. A `milestones` array in the export becomes `waypoints`. The export is a translation layer; the schema stays where it is. This is the export-side application of the internal/external vocabulary split already in use for build/shape and now milestone/waypoint.

ADR: `docs/decisions/0006-waypoints-and-export-labels.md`.

### Reflections surface, cycle-close ceremony, and bottom-nav rename (May 2026)

The Log page is renamed **Reflections** and repositioned from "diagnostics" to *the mirror where the user adjusts themselves* — the strategic reflection surface where change happens. The Today page is renamed **Motions** (with a checkbox icon in the bottom nav). The four bottom-nav surfaces become noun-shaped and role-clear: **Motions (ritual) / Swells (strategy) / Reflections (reflection — change) / Settings (configuration).**

**Cycle-close ceremony.** A new ceremony lives on Reflections, fired by the bottom-nav tab itself when a cycle closes (other tabs dim, Reflections animates with tide-line motion). One shape across four cadences (weekly / monthly / quarterly / yearly): *"What did you expect to see this [cycle]?"* → closed-cycle radar reveal → *"What did you see?"* → *"Want to tune something?"* with Swells / Motions / Skip. Both text prompts optional and individually skippable. The MI work happens in the gap between expectation and observation; the radar is the third-party witness that holds the data so Onduler doesn't have to. Cadence is the depth knob — same two prompts at every cadence, no per-cadence question banks.

**First-time-only locks.** Each cadence is locked until the user has lived through one full calendar instance of it AND met an engagement floor: 3 / 8 / 25 / 80 days logged for weekly / monthly / quarterly / yearly. Once unlocked, stays unlocked for the chapter. Calendar-anchored ("we're all in time together") rather than user-anchored — every Onduler user crosses the same Sunday, the same Dec 31. Locked page is **vibe-only**: blurred radar silhouette, moving tide lines, glimpses without numbers, soft copy with no date and no engagement counter. Pure mystery; marketing site explains the pattern for users who want the mechanism.

**Wave-cycle handling.** No ceremony, no indicator if the user logged zero motions across the cycle. Soft "we'll be here when you're back on the board" copy somewhere quiet. Indicator expires on next cycle rollover — no stacking, no guilt.

**Persistent reflections journal.** Sub-surface at `/reflections/journal` — a contemplative library of every reflection across all chapters, in chronological order with chapter separators. Each ceremony entry saves the closed radar + expectation + observation + did_tune flag. `+` in the top-right of `/reflections` opens a blank free-form journal entry (default-anchored to currently-viewed cycle, editable), with optional themed prompts from a bank (the recycled MI question library, organized by theme rather than cadence).

**Archive-and-fresh-start.** Available only at the end of a cycle-close ceremony — reward reflection with a clean slate. Confirming moves the current chapter into the user's chapter library (Settings → Past chapters, browseable read-only) and starts a new chapter that re-onboards from scratch: new build pick, new swells, new motions, locked Reflections page that re-earns its unlocks. Past chapter reflections live in the journal alongside their chapter separator, but don't follow the user into the active surface of the new chapter — reset like a new user means truly reset.

**After year one.** Once all four cadences have unlocked, expose a Settings option for the user to choose how often Onduler invites them to reflect — some want weekly nudges, some want yearly only. Requires a notification system that doesn't exist yet — deferred dependency, not blocking the surface itself.

**Quarterly is a new cadence.** Calendar quarter (Q1 = Jan–Mar etc.). Quarterly target = `ceil(weekly × days_in_quarter / 7)`, same ceil-display rule.

**Schema additions.** `chapters` (first-class, one active per user) and `reflections` (chapter-scoped, both ceremony and free-form entries). Schema-scoping of existing tables to chapters is an implementation choice deferred to the implementation session — behavior requirements are: archived data is preserved and read-only browseable from Settings; the active app only sees current-chapter data; new chapter starts fully clean.

ADR: `docs/decisions/0007-reflections-surface.md` (amended by ADR 0008 for the surface rename).

### Reflections renamed to Anchors (May 2026)

Three days after ADR 0007 shipped the Reflections surface, testing-session feedback surfaced a stronger surface name: **Anchors**. The cycle-close-and-notice surface formerly named Reflections is renamed to **Anchors** — both the surface itself and the entry-level word (an "anchor" is what the cycle-close ceremony writes; `+` "drops an anchor" as a free-form entry; the journal sub-route is "your anchors"). The framing comes from Josh's testing notes: *"Your swells anchor you to who you are as a person. If you drift away from them with your motions you become out of sync."*

The bottom-nav surfaces become: **Motions / Swells / Anchors / Settings.** The anchor icon already in place on the Reflections tab carries forward unchanged. The user-facing surf vocabulary expands from five terms (Tide / Wave / Swell / Motion / Waypoint) to six with Anchor added — Anchor sits cleanly inside the water-family vocabulary without expanding the niche-lingo load.

**Internal/external split preserved.** Schema table `reflections` does **not** rename — same pattern already in use for `milestones`/Waypoint and `builds`/shape. Code identifiers (`app/actions/reflections.ts`, `getWeekCeremonyState`, etc.) stay put. The route `/reflections` and directory `app/reflections/` move to `/anchors` and `app/anchors/` to match the user-facing surface — internal-vs-surface boundary is the table layer, not the routing layer.

**One column rename.** `user_settings.mvs_anchors` (the JSON store for per-shape "still showing up" motions on welcome-back from a wave) renames to **`user_settings.mvs_motions`** so the word "anchor" belongs to the surface. The Settings → Your shape page section currently labeled "Anchors editor" renames to avoid the collision (final wording TBD in implementation — likely "Still-showing-up motions").

**Two small accompaniments ship with the rename.** A quiet tertiary line on the locked Anchors page: *"Every motion leaves a wake."* And a contrast/density tuning pass on the hex `slow-breathe 4s` keyframe in `LockedPage.tsx`, which is currently imperceptible on iOS — final values tuned live during the implementation session.

**Why now.** Surface names are corrigible until users see them; Onduler has exactly one user, and that user has now used the surface and surfaced a better name. Catching the rename before any meaningful adoption is the right move. Doing it at the ADR level now (decision locked) and leaving the implementation queued (code unshipped) means the next Claude Code session has full spec and zero re-litigation when Josh is ready.

ADR: `docs/decisions/0008-reflections-renamed-to-anchors.md`.

### Wake — the actuals-only visual primitive (May 2026)

A new visual primitive, the **wake**, replaces the generic breathing hex on the locked Anchors page across all four cadences (week / month / quarter / year). The wake is the user's actuals shoulder polygon, drawn over a cycle window, stripped of wedge backdrop and target handles, rendered monochrome, with N vertices where N equals the user's swell count. Live data, every cadence, from day one — no random placeholder. A brand-new user with zero logs sees a pulsing circle; as they log, the wake coalesces from circle to N-gon, every motion pushing its swell axis outward.

**Wave-mode behavior.** The wake hides entirely (replaced by the `WaveField` ocean canvas) but reappears the moment the user logs a motion during the wave, starting from a fresh circle. Welcome-back from a wave resets to a fresh pulsing circle paired with the "every motion has an impact" copy line. The wake answers presence, not absence.

**Vocabulary expansion.** The locked surf vocabulary grows from six terms to seven with **Wake** added (Tide / Wave / Swell / Motion / Waypoint / Anchor / Wake). Sits cleanly in the water-family without surf-jargon overhead.

**Marketing reuse.** A `generateRandomWake(seed, n)` helper in `lib/wakes.ts` produces seeded random wakes for postcards, landing-page hero, and Instagram posts. Same shoulder-polygon math, fed synthetic actuals. The in-product wake is always live; the marketing wake is always seeded.

**Copy primitives.** Primary tagline: *Every motion leaves a wake.* (postcards, landing-page hero). Visual-paired caption: *Show me your wake.* (used only when the visual is right there to give context — Instagram, hero pair). Welcome-back line: *Every motion has an impact.* (in-product, paired with the fresh circle). Rejected: *What's in your wake?* — identical cadence to "What's in your wallet?" (Capital One), subconscious rhythm match undercuts the secret-club aesthetic.

No schema change; wakes are derived from existing `logs` and `motion_swells` data.

ADR: `docs/decisions/0010-wake.md`.

### Build deflation — drop "shape," Settings becomes "Starter sets" (May 2026)

ADR 0004's user-facing word **shape** is dropped. The Settings tile renames from "Your shape" to **Starter sets**. The four presets (Maker / Athlete / Wanderer / Scholar) survive as named starter sets — utilities, not identities. The secondary slot drops from UI for now (the `secondary_build` column stays in schema for possible re-introduction).

**Why.** "Your shape" implied identity ("I am a Maker"); the mechanism is closer to "we curated some swells for you." That gap between word and mechanism made the surface feel like productivity-app rhetoric. Deflating the word collapses the gap. Onduler's celebration-over-judgment posture leans away from identity claims — habit apps that frame users as types slip easily into telling users who they are, the prescription failure mode ADR 0004 explicitly warns against.

**Internal/external split preserved.** Code identifiers stay: `user_settings.primary_build`, `user_settings.secondary_build`, `lib/builds.ts`, `BUILD_PRESETS`. The internal RPG-skill-tree design heuristic stays as PROJECT.md guidance. Only the user-facing surface deflates.

**Welcome-back copy** (ADR 0004 §8). "Pick up your [shape]" → "Pick up where you left off." "Try a different shape →" → "Try a different starter set →." The `welcome_back_mode` column and ramp behavior are unchanged. ADR 0010 (the wake) replaces the welcome-back card's visual with a fresh pulsing circle.

**Removed.** The "Part of your maker's rhythm" build-context footer on the per-swell proficiency view — line was always quiet and now reads as overclaim.

No schema change.

ADR: `docs/decisions/0011-build-deflation.md`.

## Working agreements

- Sessions are numbered. Session start = recap + next step. Session end = summary + queue next session.
- The trigger phrase is **"Onduler, Onduler."**
- Not a pro dev. Walk through steps explicitly.
- Direct tone, no flattery, push back when wrong.
- Stack is locked unless something genuinely won't work.
- Surgical edits over full rewrites. A prior rewrite accidentally reverted visual design — that lesson is permanent.
- Apply vocabulary from this file consistently in all UI copy.
- Apply count-based pluralization in all UI strings.
- **Ceil display rule — points mode shows whole integers; hours mode displayed as HH:MM at 15-minute granularity.** Every number that renders (targets, actuals, ratios, percentages) is `ceil`'d for display: in **points mode**, to whole units (no decimals reach the screen); in **hours mode**, to 0.25 hr (15-minute granularity) then formatted as HH:MM — a 30-min target reads as "0:30"; a 45-min walk reads as "0:45". Decimal-hours display (e.g. "0.75 hr") is banned; the colon format matches how users think about time. Hour inputs accept both HH:MM ("0:30") and decimal ("0.5") via `parseHoursInput()` in `lib/periods.ts`. Underlying state stays precise floats — radar polygon geometry uses unrounded floats so vertices land in the right pixel — only the display rounds. `ceil` rather than `round` because the slight upward bias matches the celebration-leaning posture. Use `ceilDisplay(n, isHours)` for rounding, `formatHrs(n)` from `lib/format.ts` for HH:MM display, and `monthlyTargetDisplay` / `lifetimeTargetDisplay` from `lib/periods.ts` for period targets. Allocation-percentage displays in the motion detail sheet stay on `Math.round` — proportional inventory that must sum to 100, not celebration metric. Applies app-wide. See ADR 0005.
- Follow the mirror principle in reports and empty states.
- **Swells are nouns, motions are verbs.** This is the teaching pattern that snaps the whole model into place. Apply it in onboarding copy, empty states, and any UI hint that orients a new user. Swell name suggestions are noun-shaped life areas (Movement, Home, Nutrition, Family, Creativity, Reflection, Work, Travel, etc.); motion name suggestions are verb-shaped actions (kayak, cook, decorate, journal, make a playlist). See ADR 0002.
- **Internal design heuristic: skill-tree investment.** Onduler is internally modeled as a points-and-skill-tree system applied to real life — users invest in swells to build the kind of person they want to be. This is an internal design language, **not user-facing vocabulary**. Evaluate new features against the question: "does this make investing in your build feel rewarding without tipping into guilt or performance?" The surf-leaning voice, Tide/Wave/Swell/Motion/Waypoint words, and celebration-over-judgment posture stay locked at the user-facing surface. The RPG-style frame helps the designer reason about progression, builds, and "where am I over-investing this week" — it never appears in copy.
- **Builds are suggestions, never prescriptions.** Any build/shape feature — preset packs, suggested motions, waypoint templates, target recommendations — curates the menu of what counts and never weights how much specific motions count. The build can propose adding self-care motions to a Caretaker shape; it cannot say "your self-care motions are worth more than someone else's." Two firewalls: (a) builds expand the menu, never modify motion math; (b) every build proposal is individually opt-out-able with full visibility before commit. The user is always the author; the build is a tray of starting points. The minute a build feature drifts toward "you should be doing X," it has slipped into the prescription failure mode and needs to be reworked. See ADR 0004.
- **Starter sets are utilities, not identities.** The user-facing build surface is **Starter sets** (formerly "Your shape"). The four presets (Maker / Athlete / Wanderer / Scholar) are named curated swell lists — starting points, never claims about who the user is. User-facing copy never frames the user as a type ("I am a Maker"). Welcome-back card no longer references a shape — it says "Pick up where you left off." The internal `build` identifiers stay in code; the internal RPG-skill-tree design heuristic stays as a design lens. See ADR 0011.
- **Onduler's voice is witness, not coach.** The product never tells the user what they should do, never offers behavioral nudges the user didn't initiate, never generates "insights" that imply judgment. The Anchors surface is the user noticing themselves; Onduler holds the mirror. Any feature that drifts toward coaching — AI suggestions, behavioral prompts that aren't user-authored, pattern callouts that read as "you should be doing X more" — violates this rule, regardless of revenue potential or competitive pressure. The competition optimizes by becoming smarter coaches; Onduler optimizes by becoming a steadier witness. This stance is monetization-protected: features behind future paywalls must still pass this test. Aligning with behavioral-health science (see deferred work) is a credibility upgrade, not a license to start coaching.
- **No em dashes in user-facing copy.** All UI strings, empty states, hint cards, ceremony copy, button labels, error messages, and marketing surfaces use periods, commas, or restructured sentences instead of em dashes (—). Em dashes read as essay-voice or too-formal; Onduler's voice is closer to handwritten and plainspoken. Same family as the paper-list aesthetic. Applies to all new copy; existing strings get swept in dedicated polish passes. Internal docs (PROJECT.md, ADRs, specs in `docs/`) and code comments are not bound by this rule.
- **The wake responds to action, never to inaction.** The wake — the live actuals-only polygon on the locked Anchors page across all four cadences — is always present when the user is showing up and hidden when they're not. It disappears during a wave but reappears the moment any motion is logged within the wave. Brand-new users see a pulsing monochrome circle that coalesces into the polygon as they log. The visual is the literal embodiment of "every motion has an impact" — every logged motion bulges its swell axis outward in real time. Don't add scaffolding (color coding, target overlays, badges, drag handles) that would dilute the form. Monochrome on the deep-ocean ground is the rule. Marketing-side wakes are seeded random and produced by the same shoulder-polygon helper, never live. See ADR 0010.
- **Data exports use user-facing vocabulary, not schema column names.** Exports (the current JSON download at `/api/export`, and any future format) label data with the words the user sees in the UI — Waypoint, Swell, Motion, Group, Tide, Wave — not internal table names like `milestones` or `motion_swells`. The schema and code identifiers stay where they are; the export is a translation layer. When a user opens their exported file, they should recognize the words. Same internal/external split already used for build/shape and milestone/waypoint. See ADR 0006.
- **Bottom nav is the primary navigation on mobile; back is for sub-routes only.** Top-level pages reached via bottom nav (Motions, Swells, Anchors, Settings) hide their back button at mobile widths — users navigate via bottom nav. Sub-routes (motion detail sheet, inline edit forms, Add Motion/Swell/Group, Settings group-edit, `/anchors/journal`) keep their back/cancel because there's no bottom-nav route back to them. Desktop shows back everywhere; mobile shows back only where the bottom nav can't get you back. (Motions, Anchors are formerly Today, Log/Reflections — see ADRs 0007 and 0008.)
- **Every tap that triggers cross-page navigation needs press feedback at 0ms.** Bottom nav items, back/cancel links, and any "go somewhere" control must respond visually the instant the user touches them (`active:scale-[0.97]`). Motion rows and other in-place tappables (log/unlog, chip toggles inside a sheet) use a subtler dip — `active:scale-[0.985]` 0ms — so the row reads as a confirming nudge rather than a button press. Otherwise the inherent server-round-trip latency reads as broken. The press feedback doesn't make navigation faster; it makes the user feel heard. **Canonical values: nav / cross-page = `0.97`, Motion-row / in-place log = `0.985`, both 0ms.** Apply via Tailwind's `active:` variant, not via a CSS transition — instant means instant.
- **Tracking currency is app-wide, not per-swell.** The user picks Points or Hours once in Settings — that choice flows to every surface in the app: daily progress bar, daily goal, swell targets, log/report stats. There's no mixed state where some swells are tracked in points and others in hours. Both `default_points` and `default_hours` are always populated on every motion (default 1 each), and both are recorded on every log row, so switching modes later doesn't lose data — only the displayed currency changes. Swell target defaults are **weekly and motion-derived** (2026-06-12): when feeding motions are known at creation, target = 4 logs/wk of each in points mode, 3 logs/wk in hours mode; motionless and bare-created swells fall back to a 4 pts / 3 hrs floor (matching one default motion, so an empty swell never outranks a fed one). Users tune up or down from there based on what they want to feed and how heavily.
- **Swells run on a Sun→Sat weekly cycle. One mode only.** Every swell has a weekly target. Celebration fires the moment cumulative weekly progress crosses the target. The cycle runs Sunday 12:00 AM → Saturday 11:59 PM Pacific — ceremonies unlock and the new week begins on Sunday morning when the user is ready to be reflective. There is no "lifetime target, fire once, done" mode; that mental model is gone. Cycle state is *derived* from `logs.logged_at` and the Sun→Sat week definition — not stored on the swell. Monthly views are a view-only zoom (four weekly bars stacked or summed); the celebration cycle stays weekly. A missed week is a wave, not a punishment.
- **Add-anything uses the keyboard-takes-over pattern, not bottom sheets.** Anywhere the user creates a new entity — motion, swell, group — tapping the "+" affordance immediately focuses a text input and the mobile keyboard fills the bottom half of the screen. The form (name field, optional secondary fields like pts/hrs/color) sits compactly above the keyboard. No modal chrome, no sheet animation, no header bar. The keyboard *is* the experience. Cancel via system back gesture or a small cancel control. The form should feel fast enough that the user can add five motions in fifteen seconds without thinking about the UI. Bottom sheets are reserved for editing existing entities (motion detail sheet), not for creation.
- **List-row visual treatment doesn't escalate to card chrome just to add interactivity.** A list of items rendered as plain rows (e.g., the group list under the Settings → Groups toggle) stays as plain rows even when each row becomes tappable to edit. Don't add borders, elevation, shadows, or container backgrounds to "signal" interactivity. The cursor change, subtle hover state, and inherent affordance of a list row are sufficient. This is part of the paper-list aesthetic — fewer visual containers, more breathing room.
- **Smart color defaults from the active theme palette.** Anywhere the UI offers a color picker (group color, swell color, future surfaces), the *default* color is randomly selected from the current theme's accent palette — not a hardcoded gray fallback. The user can always change it, but the default should set them up for success: pick a new group, get a color that already harmonizes with the theme they chose. This applies retroactively to any current color-pickable surface and prospectively to any new one.
- **Motions page is a paper-list aesthetic.** Each motion row shows only its name, its points (or hours), and its checked state. Submotions, swell badges, group labels, and any other metadata are hidden until you open the detail sheet. The top of the page (date, score, daily progress bar) stays similarly minimal. Think: a to-do list someone wrote on a piece of paper, tracking their day's progress cleanly. Future feature work that introduces new metadata defaults to *not* surfacing it on the main row — if it needs to be visible by default, that's a deliberate decision and should be justified. The bottom-nav checkbox icon reinforces this — Motions is where you check things off. (Formerly Today — see ADR 0007.)
- **Motions is the daily ritual surface; Swells is the strategic surface; Anchors is where you notice and re-align — the surface where you drop a marker against drift; Settings is configuration.** Motions stays paper-light because it's where the user shows up every day to tap motion boxes and move on — any structural complexity introduced here would break the ritual. Swells is where structural depth belongs: per-swell proficiency views, motion constellations, waypoints, time-view zooms, build context. Anchors is where the user notices and re-aligns — cycle-close ceremonies, the radar with drag-to-tune, the persistent anchors journal, and the only path to archive-and-fresh-start. Settings carries one-time configuration. The user goes to Swells to *tune their shape*; they go to Motions to *do the work*; they go to Anchors to *notice drift and re-align*; they go to Settings to *configure*. This four-surface role split — ritual / strategy / re-alignment / configuration — is how Onduler scales as users invest more without ever becoming exhausting. New features get evaluated against this split — "which surface does this belong on, by role?" is a real question. (Motions, Anchors are formerly Today, Log/Reflections — see ADRs 0007 and 0008. See also ADR 0004 for the original Today / Swells split rationale.)
- **Skip is always a door.** Every prompt, ceremony, or guided flow Onduler puts in front of the user is skippable end-to-end. No required text fields anywhere in the cycle-close ceremony, no forced answers in onboarding, no "you must complete this before continuing." This is the celebration-over-judgment posture stated as a UX rule. When designing a new flow, the first question is "what does the skip path look like, and does it leave the user in a good place?" If the answer is awkward, the flow needs to be rethought, not the skip removed. See ADR 0007.
- **Points as time-budget; hours as honest accounting.** Points is the expressive tracking mode where users weight motivation by inflating the point value of motions their future-self values more than their present-self wants to do (e.g., a workout's points get inflated to economically reward the action that earns the joy of the beach photo). Hours is the honest tracking mode where time is what it is — no inflation lever — for users who want literal accounting rather than gamified weighting. The framing behind points: "you have a limited amount of time, you're already spending it on something, points let you decide how much each kind of spending matters to your future self." This framing lives in PROJECT.md, in onboarding pedagogy, and (eventually) in marketing voice; it **never appears as a UI label**. Vocabulary lock holds: Tide, Wave, Swell, Motion, Group, Waypoint. *Budget* is a frame, not a vocabulary item. Inflation is taught at the front end of onboarding; a cross-mode tutorial gut-check ("you're tracking in points; here's how hours mode works, in case you'd rather see it that way" and vice versa) surfaces later in the user's lifecycle. Waypoint bonuses extend the frame internally as windfall income but no budget language appears in waypoint UI. See ADR 0007.
- **Always commit after completing work.** At the end of every task or logical chunk of work, create a git commit with a clear message. After committing, tell Josh how to push (e.g., `git push` or `git push -u origin <branch>`). Don't wait to be asked.
- When a Vercel deploy *should* have picked up changes but behavior suggests it didn't — especially after a schema migration — run `vercel --prod --force` to skip the build cache. Vercel's build cache can retain stale lambda artifacts even when source has changed, and SSR queries silently return empty against the old shape. Verified incident: May 2026 schema migration from `motion_groups` junction to `motions.group_id` FK. The deployed build kept restoring cache from a pre-migration build until `--force` was used. Always rebuild fresh after a schema shape change.
- **dnd-kit sortable lists need `touch-action: none` on iOS, not `touch-action: manipulation`.** Safari's native gesture arbitration claims `manipulation`-marked elements for scroll before dnd-kit's PointerSensor activates, so long-press silently falls back to tap — drag attempts trigger the tap handler instead of reordering (in our case: the motion logs instead of moving). `touch-action: none` cedes the gesture entirely to JS, which is what dnd-kit needs and recommends. Tradeoff is intentional: users can't initiate page scroll by touching a card — they scroll via the gaps/margins between rows, the standard pattern for sortable mobile lists. Verified incident: May 2026 cross-group drag rollout — programmatic pointer events worked because they bypass Safari's gesture arbitration; real fingers failed until the value was flipped from `manipulation` to `none`.
